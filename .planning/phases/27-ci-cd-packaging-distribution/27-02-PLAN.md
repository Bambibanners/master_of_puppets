---
phase: 27-ci-cd-packaging-distribution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/installer/install_universal.sh
  - puppeteer/installer/install_node.sh
  - puppeteer/installer/install_universal.ps1
  - puppeteer/installer/install_ca.ps1
  - docs/docs/getting-started/enroll-node.md
autonomous: true
requirements:
  - Installer scripts rebrand MoP/Master of Puppets strings to Axiom
  - Getting-started enroll-node doc features curl one-liner as primary install path
  - Docker Compose install path documented as the power-user alternative

must_haves:
  truths:
    - "No user-visible 'Master of Puppets' or 'MoP' string remains in any installer script banner, comment, or echo output"
    - "The enroll-node.md page presents the curl one-liner as the first/primary install method"
    - "The enroll-node.md page documents the /api/installer/compose?token=... endpoint"
    - "The Docker Compose install path remains documented as the power-user alternative"
  artifacts:
    - path: "puppeteer/installer/install_universal.sh"
      provides: "Rebranded universal Linux/macOS installer"
      contains: "Axiom"
    - path: "puppeteer/installer/install_node.sh"
      provides: "Rebranded Linux node installer"
      contains: "Axiom"
    - path: "puppeteer/installer/install_universal.ps1"
      provides: "Rebranded Windows PowerShell installer"
      contains: "Axiom"
    - path: "puppeteer/installer/install_ca.ps1"
      provides: "Rebranded CA installer (if user-visible MoP strings present)"
      contains: "Axiom"
    - path: "docs/docs/getting-started/enroll-node.md"
      provides: "Updated enroll-node guide with curl one-liner as hero path"
      contains: "curl -sSL"
  key_links:
    - from: "docs/docs/getting-started/enroll-node.md"
      to: "/installer.sh endpoint"
      via: "curl one-liner documentation"
      pattern: "installer.sh"
    - from: "docs/docs/getting-started/enroll-node.md"
      to: "/api/installer/compose"
      via: "compose endpoint documentation"
      pattern: "installer/compose"
---

<objective>
Rebrand the four installer scripts from "Master of Puppets"/"MoP" to "Axiom", and update the enroll-node getting-started guide to present the curl one-liner as the primary node installation path (with Docker Compose as the power-user alternative).

Purpose: Axiom is the public brand — no "Master of Puppets" strings should appear in scripts that users download and run. The curl one-liner already works but isn't prominently featured in docs.
Output: Four rebranded installer scripts + updated enroll-node.md
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-ci-cd-packaging-distribution/27-CONTEXT.md
@.planning/phases/27-ci-cd-packaging-distribution/27-RESEARCH.md

<interfaces>
<!-- Verified branding occurrences from research. Installer file paths confirmed. -->

Installer files that need rebranding (all in puppeteer/installer/):
  install_universal.sh  — banner comment "Master of Puppets - Universal Installer (v1.0)"
                        — filename refs "mop-root.crt" → "axiom-root.crt"
  install_node.sh       — banner comment "Master of Puppets - Linux Node Installer"
  install_universal.ps1 — banner comment "Master of Puppets - Universal Installer (v1.0)"
  install_ca.ps1        — PowerShell synopsis/description contains "Master of Puppets Internal Root CA"

Routing: orchestrator serves installer scripts at /installer.sh and /api/installer/compose
These routes are NOT changed in this phase.

Existing enroll-node.md structure (from docs/docs/getting-started/enroll-node.md):
  - Step 1: Generate an enrollment token
  - Step 2: Configure node connectivity
  - Step 3 onwards: Docker Compose path

The curl one-liner:
  curl -sSL https://<your-orchestrator>/installer.sh | bash -s -- --token "<JOIN_TOKEN>"

The compose endpoint:
  /api/installer/compose?token=<JOIN_TOKEN>  — serves a ready-to-run node-compose.yaml
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand installer scripts</name>
  <files>puppeteer/installer/install_universal.sh, puppeteer/installer/install_node.sh, puppeteer/installer/install_universal.ps1, puppeteer/installer/install_ca.ps1</files>
  <action>
Read each installer file and replace all user-visible "Master of Puppets" and "MoP" branding with "Axiom". Only change banners, comments, echo/print output strings, and variable/filename references. Do NOT change API route paths, URL patterns, or functional logic.

Specific changes required (verified from research):

install_universal.sh:
  - Line 2 banner: "# Master of Puppets - Universal Installer (v1.0) - Linux/macOS" → "# Axiom - Universal Installer (v1.0) - Linux/macOS"
  - Any occurrence of "mop-root.crt" (cert filename used in echo or variable) → "axiom-root.crt"
  - Any other echo/printf lines containing "Master of Puppets" or "MoP" → replace with "Axiom"

install_node.sh:
  - Line 2 banner: "# Master of Puppets - Linux Node Installer" → "# Axiom - Linux Node Installer"
  - Any other user-visible "Master of Puppets"/"MoP" strings in echo/print output

install_universal.ps1:
  - Line 1 comment: "# Master of Puppets - Universal Installer (v1.0)" → "# Axiom - Universal Installer (v1.0)"
  - Any other user-visible "Master of Puppets"/"MoP" strings in comments or Write-Host output

install_ca.ps1:
  - .SYNOPSIS line: "Imports the Master of Puppets Internal Root CA..." → "Imports the Axiom Internal Root CA..."
  - .DESCRIPTION: replace "Master of Puppets" with "Axiom" in all visible text
  - Any other user-visible "Master of Puppets"/"MoP" strings

After editing, verify no MoP/Master occurrences remain in user-visible strings (banners, comments, echo/Write-Host lines). Functional code paths, URL routes, and API endpoint strings are NOT changed.
</action>
  <verify>
    <automated>grep -r "Master of Puppets" puppeteer/installer/ 2>/dev/null && echo "FAIL: Master of Puppets still present" || echo "PASS: no Master of Puppets strings"</automated>
  </verify>
  <done>All four installer files contain no "Master of Puppets" strings. Script banners and user-visible output all say "Axiom". Functional logic is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update enroll-node.md with curl one-liner as primary path</name>
  <files>docs/docs/getting-started/enroll-node.md</files>
  <action>
Read the current `docs/docs/getting-started/enroll-node.md`. The existing content covers Steps 1-2 (token generation, connectivity config) followed by the Docker Compose path (create compose file, run container, verify).

Insert a new section after Step 2 (Configure node connectivity) and before the existing Docker Compose steps. This new section presents two install methods clearly:

**Structure to add:**

```markdown
## Step 3: Install the node

Choose your installation method:

=== "Easy — curl installer"

    The one-liner downloads and runs the universal installer script hosted on the orchestrator:

    ```bash
    curl -sSL https://<your-orchestrator>/installer.sh | bash -s -- --token "<JOIN_TOKEN>"
    ```

    Replace `<your-orchestrator>` with your orchestrator's hostname or IP (e.g., `10.0.0.5:8001` or `my-orchestrator.example.com`).

    The installer script:
    - Detects Docker or Podman on your system
    - Downloads a ready-to-run `node-compose.yaml` from the orchestrator
    - Starts the node container automatically

    !!! tip "Getting the compose file without running it"
        The orchestrator also serves the generated compose file directly if you want to inspect or customise it before running:
        ```bash
        curl -sSL "https://<your-orchestrator>/api/installer/compose?token=<JOIN_TOKEN>" > node-compose.yaml
        docker compose -f node-compose.yaml up -d
        ```

=== "Manual — Docker Compose"

    For full control over the configuration, create the compose file manually.
```

Then move the existing Docker Compose steps (Step 3 onwards — create compose file, configure environment, run container, verify) under the "Manual — Docker Compose" tab.

Renumber: what was Step 3+ becomes Step 4 (verify enrollment) after both installation paths converge.

Use MkDocs Material `===` tab syntax (requires `pymdownx.tabbed` — check mkdocs.yml; if not enabled, use a simpler two-subsection approach with H3 headers "### Option A: curl installer (recommended)" and "### Option B: Docker Compose (power user)" instead of tabs).

Check `docs/mkdocs.yml` for whether `pymdownx.tabbed` is in `markdown_extensions`. If it is, use tabs. If not, use H3 subsections — do NOT add new MkDocs extensions in this plan.

The `/api/installer/compose?token=...` endpoint must be documented in the tip admonition (as shown above). This endpoint serves a ready-to-run `node-compose.yaml` that the curl installer uses internally.

Do not remove any existing content — just reorganize and add the curl path before the existing Compose steps.
</action>
  <verify>
    <automated>python3 -c "
content = open('docs/docs/getting-started/enroll-node.md').read()
assert 'installer.sh' in content, 'Missing installer.sh reference'
assert 'installer/compose' in content, 'Missing /api/installer/compose reference'
assert 'curl' in content, 'Missing curl one-liner'
print('PASS: all required elements present')
"</automated>
  </verify>
  <done>enroll-node.md contains the curl one-liner, /api/installer/compose endpoint documentation, and the Docker Compose path. The curl path is presented before the Compose path. MkDocs build --strict passes (no new nav entries or missing files introduced).</done>
</task>

</tasks>

<verification>
1. No "Master of Puppets" strings in installer scripts: `grep -r "Master of Puppets" puppeteer/installer/`
2. curl one-liner present in enroll-node.md: `grep "installer.sh" docs/docs/getting-started/enroll-node.md`
3. Compose endpoint documented: `grep "installer/compose" docs/docs/getting-started/enroll-node.md`
4. Local mkdocs build passes (non-strict acceptable for local — strict requires Docker): `cd docs && mkdocs build 2>&1 | tail -5` (check for errors, not warnings about openapi.json)
</verification>

<success_criteria>
- All four installer scripts: zero "Master of Puppets" strings, "Axiom" in all user-visible banners
- enroll-node.md: curl one-liner is the first install option presented, /api/installer/compose documented, Docker Compose path preserved
</success_criteria>

<output>
After completion, create `.planning/phases/27-ci-cd-packaging-distribution/27-02-SUMMARY.md`
</output>
