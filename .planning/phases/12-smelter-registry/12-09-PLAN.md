---
phase: 12-smelter-registry
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - puppeteer/requirements.txt
autonomous: true
requirements:
  - SMLT-01
  - SMLT-02
  - SMLT-03
  - SMLT-04
  - SMLT-05
gap_closure: true

must_haves:
  truths:
    - "ROADMAP.md shows Phase 12 as complete with 9/9 plans and a completion date"
    - "STATE.md progress table reflects Phase 12 in completed phases list"
    - "pip-audit is present in puppeteer/requirements.txt"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 12 status row updated to Complete"
      contains: "12. Smelter Registry"
    - path: ".planning/STATE.md"
      provides: "Velocity table and completed_phases count updated"
    - path: "puppeteer/requirements.txt"
      provides: "pip-audit dependency declared"
      contains: "pip-audit"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "Phase 12 progress row"
      via: "Status column"
      pattern: "Smelter Registry.*Complete"
---

<objective>
Close out Phase 12 bookkeeping: update ROADMAP.md and STATE.md to reflect that all 8 implementation plans are complete, and add pip-audit to puppeteer/requirements.txt (the smelter_service.py calls pip-audit as a subprocess but it was never added to the declared dependencies).

Purpose: Tracking files still say Phase 12 is "Not started" and the pip-audit binary is not declared as a dependency, which would cause the smelter CVE scan to fail on a fresh install.
Output: Accurate tracking files + pip-audit in requirements.txt.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pip-audit to requirements.txt</name>
  <files>puppeteer/requirements.txt</files>
  <action>
Append `pip-audit` as a new line at the end of `puppeteer/requirements.txt`.

The smelter_service.py runs `pip-audit` as a subprocess to perform CVE scanning (SMLT-02). The binary must be installed in the same environment as the agent service. It is currently absent from the declared dependencies, which means a fresh `pip install -r requirements.txt` would not install it and the CVE scan endpoint would fail with "command not found".

No version pin is needed — pip-audit follows semver and breaking changes are rare. Consistent with the rest of this file (no version pins on most packages).
  </action>
  <verify>
    <automated>grep "pip-audit" /home/thomas/Development/master_of_puppets/puppeteer/requirements.txt</automated>
  </verify>
  <done>pip-audit appears as a line in puppeteer/requirements.txt</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md — Phase 12 marked complete</name>
  <files>.planning/ROADMAP.md</files>
  <action>
In `.planning/ROADMAP.md`, make the following targeted edits:

1. In the `### 🚧 v7.0` phase list, change:
   ```
   - [ ] **Phase 12: Smelter Registry** — Vetted ingredient catalog, CVE scanning, STRICT/WARNING enforcement
   ```
   to:
   ```
   - [x] **Phase 12: Smelter Registry** — Vetted ingredient catalog, CVE scanning, STRICT/WARNING enforcement (completed 2026-03-15)
   ```

2. In the Progress table, change the Phase 12 row:
   ```
   | 12. Smelter Registry | v7.0 | 0/TBD | Not started | - |
   ```
   to:
   ```
   | 12. Smelter Registry | v7.0 | 9/9 | Complete | 2026-03-15 |
   ```

3. In the `### Phase 12: Smelter Registry` detail block, change:
   ```
   **Plans**: TBD
   ```
   to:
   ```
   **Plans**: 9 plans
   Plans:
   - [x] 12-01-PLAN.md — Wave 0 test stubs
   - [x] 12-02-PLAN.md — DB model + CRUD API
   - [x] 12-03-PLAN.md — SmelterService implementation
   - [x] 12-04-PLAN.md — pip-audit CVE scan integration
   - [x] 12-05-PLAN.md — Foundry enforcement (STRICT/WARNING)
   - [x] 12-06-PLAN.md — Template compliance badging (backend)
   - [x] 12-07-PLAN.md — Dashboard compliance badges (frontend)
   - [x] 12-08-PLAN.md — Phase verification
   - [x] 12-09-PLAN.md — Bookkeeping wrap-up
   ```

Do not modify any other section of ROADMAP.md.
  </action>
  <verify>
    <automated>grep -A2 "Smelter Registry" /home/thomas/Development/master_of_puppets/.planning/ROADMAP.md | grep -E "Complete|complete|2026-03-15"</automated>
  </verify>
  <done>ROADMAP.md progress table shows Phase 12 as Complete with date 2026-03-15; phase list checkbox is checked</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md — Phase 12 velocity entry and completed count</name>
  <files>.planning/STATE.md</files>
  <action>
In `.planning/STATE.md`, make the following targeted edits:

1. In the `## Performance Metrics` / `**By Phase:**` table, add a row for Phase 12 after the Phase 11 rows. Use the same format as existing rows:
   ```
   | Phase 12-smelter-registry | 9 plans | ~3 hours | ~20 min/plan |
   ```

2. In the frontmatter, increment `completed_phases` from `8` to `9`:
   ```yaml
   completed_phases: 9
   ```

3. In the `### Decisions` section, add an entry for the key Phase 12 architectural decisions:
   ```
   - [Phase 12-smelter-registry]: pip-audit runs with --no-deps --disable-pip flags to avoid venv creation inside the agent container
   - [Phase 12-smelter-registry]: STRICT mode returns HTTP 403 blocking the build; WARNING mode sets is_compliant=False on the template and continues
   - [Phase 12-smelter-registry]: smelter_enforcement_mode stored in Config table (key/value) — no new table needed
   ```

Do not modify any other section of STATE.md.
  </action>
  <verify>
    <automated>grep "completed_phases" /home/thomas/Development/master_of_puppets/.planning/STATE.md</automated>
  </verify>
  <done>STATE.md frontmatter shows completed_phases: 9; Phase 12 velocity row present in metrics table; three Phase 12 decisions recorded</done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. pip-audit is in requirements.txt: `grep pip-audit puppeteer/requirements.txt` returns a match
2. ROADMAP progress table shows Phase 12 complete: `grep "Smelter Registry" .planning/ROADMAP.md` shows "Complete" and "2026-03-15"
3. STATE.md completed count is 9: `grep "completed_phases" .planning/STATE.md` returns `completed_phases: 9`
4. No other files were modified (these are bookkeeping-only changes)
</verification>

<success_criteria>
- pip-audit declared in requirements.txt so fresh installs can run CVE scans
- ROADMAP.md accurately reflects Phase 12 as complete (9 plans, 2026-03-15)
- STATE.md completed_phases incremented to 9, velocity row added, key decisions recorded
- All changes are purely additive — no implementation code touched
</success_criteria>

<output>
After completion, create `.planning/phases/12-smelter-registry/12-09-SUMMARY.md` recording:
- pip-audit added to requirements.txt
- ROADMAP.md Phase 12 row updated to Complete
- STATE.md completed_phases incremented to 9
</output>
