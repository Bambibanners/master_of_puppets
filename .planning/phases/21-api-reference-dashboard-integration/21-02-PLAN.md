---
phase: 21-api-reference-dashboard-integration
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/dashboard/src/AppRoutes.tsx
  - puppeteer/dashboard/src/layouts/MainLayout.tsx
autonomous: false
requirements:
  - DASH-01
  - DASH-02

must_haves:
  truths:
    - "The sidebar shows a 'Docs' link that opens /docs/ in a new browser tab"
    - "Navigating to the React-internal /docs path redirects to / rather than showing Docs.tsx"
    - "Docs.tsx and UserGuide.md no longer exist in the codebase"
    - "npm run build completes without TypeScript errors (no dangling Docs import)"
  artifacts:
    - path: "puppeteer/dashboard/src/AppRoutes.tsx"
      provides: "Routes without Docs route, with catch-all Navigate redirect"
    - path: "puppeteer/dashboard/src/layouts/MainLayout.tsx"
      provides: "Sidebar with external <a> Docs link opening /docs/ in new tab"
  key_links:
    - from: "puppeteer/dashboard/src/layouts/MainLayout.tsx"
      to: "/docs/"
      via: "plain <a> tag with target=_blank"
      pattern: "href=\"/docs/\""
    - from: "puppeteer/dashboard/src/AppRoutes.tsx"
      to: "React Router Navigate"
      via: "catch-all Route path=* element=Navigate to=/"
      pattern: "path=\"\\*\""
---

<objective>
Remove the in-app Docs view (Docs.tsx, UserGuide.md) from the React dashboard and replace
it with an external link in the sidebar that opens /docs/ in a new tab. Add a catch-all
React Router redirect so navigating to any unknown path (including the old /docs route)
redirects to the root dashboard.

Purpose: Delivers DASH-01/02 — documentation is consolidated on the MkDocs site, the
React app no longer renders inline markdown, and the sidebar provides a visible Docs link.
Output: Updated AppRoutes.tsx and MainLayout.tsx. Two files deleted (Docs.tsx, UserGuide.md).
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Key contracts the executor needs from the current codebase. -->

From puppeteer/dashboard/src/AppRoutes.tsx (current state):
  - Line 13: const Docs = lazy(() => import('./views/Docs'));
  - Line 49: <Route path="docs" element={<Docs />} />
  - No existing catch-all route (path="*")
  - All app routes are nested under <Route path="/" element={<PrivateRoute>...}>

From puppeteer/dashboard/src/layouts/MainLayout.tsx (current state):
  - NavItem component: uses React Router NavLink — cannot open external URLs
  - SidebarContent renders NavItem for all routes under <nav className="space-y-1.5">
  - System section (line 172): Settings, Users & Roles, Service Principals, Webhooks, Audit Log
  - No existing Docs entry in the sidebar nav
  - lucide-react icons already imported: LayoutDashboard, Network, Server, ShieldCheck,
    Settings, Menu, Cpu, Boxes, ScrollText, Users, KeyRound, CalendarClock, Bot, Webhook,
    HistoryIcon — BookOpen is NOT currently imported
  - Inactive NavItem CSS: "text-zinc-400" + "hover:bg-zinc-800 hover:text-white"

Files to DELETE:
  - puppeteer/dashboard/src/views/Docs.tsx
  - puppeteer/dashboard/src/assets/UserGuide.md
  (Docs.tsx is the only consumer of UserGuide.md per research)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Docs route from AppRoutes.tsx and add catch-all redirect</name>
  <files>puppeteer/dashboard/src/AppRoutes.tsx</files>
  <action>
In puppeteer/dashboard/src/AppRoutes.tsx:

1. Delete line: `const Docs = lazy(() => import('./views/Docs'));`

2. Delete line: `<Route path="docs" element={<Docs />} />`

3. Add import at top of file (after existing React Router imports):
   `import { Routes, Route, Navigate } from 'react-router-dom';`
   (Navigate is already exported from react-router-dom — just add it to the import)

4. Add catch-all route as the LAST child inside the `<Route path="/">` nesting,
   after all existing named routes:
   `<Route path="*" element={<Navigate to="/" replace />} />`

The catch-all must be inside the PrivateRoute-wrapped parent, not outside it, so
unauthenticated users hitting unknown paths still see the Login redirect, not a
bare redirect to /.

After editing, run TypeScript check:
  cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npm run build

Fix any TypeScript errors before proceeding.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npm run build 2>&1 | tail -5 && grep -L "Docs" src/AppRoutes.tsx && echo "PASS: build clean, Docs removed from AppRoutes"</automated>
  </verify>
  <done>npm run build completes with no errors. AppRoutes.tsx contains no reference to Docs. A catch-all Route with Navigate to="/" exists.</done>
</task>

<task type="auto">
  <name>Task 2: Add external Docs link to sidebar, delete Docs.tsx and UserGuide.md</name>
  <files>puppeteer/dashboard/src/layouts/MainLayout.tsx</files>
  <action>
In puppeteer/dashboard/src/layouts/MainLayout.tsx:

1. Add BookOpen to the lucide-react import (it is not currently imported):
   Find: `import { LayoutDashboard, Network, Server, ...`
   Add `BookOpen` to the destructured imports.

2. In SidebarContent, add the external Docs link AFTER the last NavItem in the System
   section (after the Audit Log NavItem, before the closing </nav>):

   ```tsx
   <div className="pt-4 pb-1 px-3 text-2xs font-bold text-zinc-500 uppercase tracking-widest">
     Documentation
   </div>
   <a
     href="/docs/"
     target="_blank"
     rel="noopener noreferrer"
     className="flex items-center gap-3 rounded-lg px-3 py-2 text-sm font-medium transition-all hover:bg-zinc-800 hover:text-white text-zinc-400"
     aria-label="Documentation"
   >
     <BookOpen className="h-4 w-4 shrink-0" />
     <span>Docs</span>
   </a>
   ```

   The CSS class string exactly matches the inactive-state NavItem style so it
   visually blends with the sidebar. It will never be "active" (no React Router match).

3. Delete these two files entirely:
   - puppeteer/dashboard/src/views/Docs.tsx
   - puppeteer/dashboard/src/assets/UserGuide.md

   Use the Bash tool: `rm` both files.

4. After all edits, run the build again to confirm no errors:
   cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npm run build
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npm run build 2>&1 | tail -5 && ! test -f src/views/Docs.tsx && ! test -f src/assets/UserGuide.md && grep -q 'href="/docs/"' src/layouts/MainLayout.tsx && echo "PASS: build clean, Docs.tsx deleted, external link present"</automated>
  </verify>
  <done>npm run build completes with no errors. Docs.tsx and UserGuide.md are deleted. MainLayout.tsx contains a plain &lt;a&gt; tag with href="/docs/" target="_blank". BookOpen is imported from lucide-react.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify sidebar Docs link and removed React route</name>
  <what-built>
    React dashboard cleanup: Docs.tsx and UserGuide.md deleted, React Router /docs route
    removed with catch-all redirect to /, and sidebar now shows an external Docs link
    that opens /docs/ in a new tab.
  </what-built>
  <how-to-verify>
    1. Start the dev server: cd puppeteer/dashboard && npm run dev
    2. Log in to the dashboard
    3. Verify: "Docs" entry is visible in the sidebar under a "Documentation" section
    4. Verify: Clicking "Docs" opens https://dev.master-of-puppets.work/docs/ (or /docs/)
       in a NEW TAB, not in the current tab
    5. Verify: Manually navigate to http://localhost:5173/docs in the current tab —
       it should redirect to the dashboard root (/) rather than showing an old markdown view
    6. Verify: All other sidebar navigation links still work correctly
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues</resume-signal>
  <action>Human verification — follow how-to-verify steps above.</action>
  <verify><automated>echo "human-checkpoint"</automated></verify>
  <done>User confirms all how-to-verify steps passed and types approved.</done>
</task>

</tasks>

<verification>
Overall phase checks for Plan 21-02:
- `cd puppeteer/dashboard && npm run build` exits 0
- `! test -f puppeteer/dashboard/src/views/Docs.tsx` — file does not exist
- `! test -f puppeteer/dashboard/src/assets/UserGuide.md` — file does not exist
- `grep -q 'href="/docs/"' puppeteer/dashboard/src/layouts/MainLayout.tsx` — link present
- `grep -q 'target="_blank"' puppeteer/dashboard/src/layouts/MainLayout.tsx` — new tab attribute present
- `! grep -q "Docs" puppeteer/dashboard/src/AppRoutes.tsx` — no Docs reference in routes
- `grep -q 'path="\*"' puppeteer/dashboard/src/AppRoutes.tsx` — catch-all route present
</verification>

<success_criteria>
1. npm run build completes with zero TypeScript errors
2. Docs.tsx and UserGuide.md are gone from the repository
3. Navigating to /docs in the React app redirects to /
4. The sidebar Docs link is a plain &lt;a&gt; tag pointing to /docs/ with target="_blank"
5. All other sidebar items and routes are unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/21-api-reference-dashboard-integration/21-02-SUMMARY.md`
</output>
