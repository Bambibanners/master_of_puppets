---
phase: 11-compatibility-engine
plan: "06"
type: execute
wave: 4
depends_on:
  - "11-04"
  - "11-05"
files_modified: []
autonomous: false
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04

must_haves:
  truths:
    - "Every tool in the CapabilityMatrix has an os_family field (DEBIAN/ALPINE) visible in the Tools tab"
    - "Tool records show runtime_dependencies list in the Tools tab"
    - "Submitting a RUNTIME blueprint with an ALPINE-incompatible tool shows a 422 error message in the UI"
    - "The Foundry blueprint editor filters tools in real-time when OS family is changed"
    - "OS family badge appears on runtime blueprint cards"
  artifacts:
    - path: "puppeteer/tests/test_compatibility_engine.py"
      provides: "All 5 tests green"
    - path: "puppeteer/migration_v26.sql"
      provides: "Applied to running DB"
  key_links:
    - from: "Browser Foundry → Tools tab"
      to: "GET /api/capability-matrix?include_inactive=true"
      via: "Live HTTP call visible in network tab"
      pattern: "include_inactive=true"
    - from: "Browser Foundry → Create Runtime Blueprint → OS family dropdown"
      to: "GET /api/capability-matrix?os_family=DEBIAN"
      via: "TanStack Query refetch on OS change"
      pattern: "os_family=DEBIAN"
---

<objective>
Human verification of the complete Compatibility Engine implementation. The user exercises all four COMP requirements in a live browser against the running stack.

Purpose: Gate before phase closure — confirms the backend enforcement and frontend filtering work end-to-end in a real environment, not just in test assertions.
Output: Human sign-off that all four success criteria from the phase goal are met.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-compatibility-engine/11-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite + apply migration to running stack</name>
  <files></files>
  <action>
1. Run the full backend test suite to confirm all 5 phase tests pass and no regressions:
```bash
cd /home/thomas/Development/master_of_puppets/puppeteer && pytest -v 2>&1 | tail -20
```

2. Apply migration_v26.sql to the running Docker stack database:
```bash
docker exec -i puppeteer-db-1 psql -U puppeteer -d puppeteer < /home/thomas/Development/master_of_puppets/puppeteer/migration_v26.sql
```
(If the DB container name differs, check: `docker ps | grep postgres`)

3. Rebuild and restart the agent service to pick up code changes:
```bash
docker compose -f /home/thomas/Development/master_of_puppets/puppeteer/compose.server.yaml build agent && docker compose -f /home/thomas/Development/master_of_puppets/puppeteer/compose.server.yaml up -d --no-build agent
```

4. Rebuild the dashboard (or confirm dev server is running):
```bash
cd /home/thomas/Development/master_of_puppets/puppeteer/dashboard && npm run build 2>&1 | tail -5
```

Report the test results and migration output to the user.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && pytest tests/test_compatibility_engine.py -v 2>&1 | tail -10</automated>
  </verify>
  <done>All 5 COMP tests pass. Migration applied successfully. Agent service restarted without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Compatibility Engine — Phase 11 implementation:
- COMP-01: CapabilityMatrix entries have OS family + is_active metadata (Tools tab in Foundry)
- COMP-02: Runtime dependencies stored as JSON list on each tool entry (visible in Tools tab)
- COMP-03: POST /api/blueprints rejects OS-mismatched tools with 422 + offending_tools list; dep-confirmation flow via confirmed_deps
- COMP-04: CreateBlueprintDialog filters tools in real-time based on selected OS family; placeholder shown before OS is selected
  </what-built>
  <how-to-verify>
Open the Foundry page in the dashboard (https://localhost:8001 or the configured URL).

**Step 1 — Tools tab (COMP-01, COMP-02):**
- Click the "Tools" tab in the Foundry page
- Confirm the table loads and shows existing tool entries (python-3.11, scapy-2.5, etc.)
- Confirm each row has an OS Family badge (DEBIAN or ALPINE colored)
- Confirm the "Runtime Deps" column shows chips or "none"
- Click "Add Tool Entry" — confirm the dialog opens with all fields (Tool ID, OS Family, Validation Cmd, Recipe, Runtime Deps)
- Close without saving

**Step 2 — OS family filter (COMP-04):**
- Go to the "Runtime Blueprints" tab, click "New Runtime Blueprint"
- Confirm the dialog has an "OS Family" dropdown (DEBIAN / ALPINE) above the Base OS select
- Confirm the tool list section shows the placeholder "Select an OS family to see available tools" before selecting OS
- Select "DEBIAN" — confirm the tool list updates to show DEBIAN tools only
- Select "ALPINE" — confirm the tool list updates to show ALPINE tools only (or shows "No tools available" if no ALPINE tools are seeded)
- Switch back and forth between DEBIAN and ALPINE — confirm previously selected tools are cleared on change

**Step 3 — OS mismatch rejection (COMP-03):**
- With "ALPINE" selected as OS Family, select a DEBIAN-only tool (e.g. python-3.11) if it appears (it shouldn't — the filter should prevent it)
- Instead: confirm that the tool list for ALPINE does NOT show DEBIAN-only tools
- To test the API rejection directly, you can use curl or the browser network tab if needed

**Step 4 — Blueprint OS family badge (COMP-01):**
- View the Runtime Blueprints list
- If any blueprints exist, confirm they show an OS family badge (DEBIAN/ALPINE)
- Create a new RUNTIME blueprint with OS Family = DEBIAN — confirm it appears with a DEBIAN badge after creation

Type "approved" if all four checks pass, or describe any issues found.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or describe issues for gap closure</resume-signal>
</task>

</tasks>

<verification>
All four COMP requirements verified by human inspection in the live dashboard.
Full pytest suite green.
</verification>

<success_criteria>
- All 5 backend tests pass
- Migration v26 applied to running DB
- Human confirms Tools tab shows OS family + runtime deps metadata (COMP-01, COMP-02)
- Human confirms tool list filters in real-time by OS family (COMP-04)
- Human confirms OS family badge appears on blueprint cards (COMP-01)
- Human confirms ALPINE-incompatible tools are absent from ALPINE tool list (COMP-03 UI side)
</success_criteria>

<output>
After completion, create `.planning/phases/11-compatibility-engine/11-06-SUMMARY.md`
</output>
