---
phase: 26-axiom-branding-community-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .github/ISSUE_TEMPLATE/bug_report.md
  - .github/ISSUE_TEMPLATE/feature_request.md
  - .github/pull_request_template.md
  - CODE_OF_CONDUCT.md
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02

must_haves:
  truths:
    - "Running `pip install -e .` makes `axiom-push` available as a CLI command"
    - "GitHub renders two issue templates (Bug Report, Feature Request) when opening a new issue"
    - "GitHub auto-populates PR template body for all new pull requests"
    - "CODE_OF_CONDUCT.md exists at repo root with Contributor Covenant v2.1 text"
  artifacts:
    - path: "pyproject.toml"
      provides: "axiom-push CLI entry point"
      contains: "axiom-push = \"mop_sdk.cli:main\""
    - path: ".github/ISSUE_TEMPLATE/bug_report.md"
      provides: "Bug report template with front matter"
      contains: "name: Bug Report"
    - path: ".github/ISSUE_TEMPLATE/feature_request.md"
      provides: "Feature request template with front matter"
      contains: "name: Feature Request"
    - path: ".github/pull_request_template.md"
      provides: "PR checklist including /ee boundary check"
      contains: "No changes to"
    - path: "CODE_OF_CONDUCT.md"
      provides: "Contributor Covenant v2.1"
      contains: "Contributor Covenant"
  key_links:
    - from: "pyproject.toml [project.scripts]"
      to: "mop_sdk.cli:main"
      via: "axiom-push entry point"
      pattern: "axiom-push.*mop_sdk"
---

<objective>
Rename the CLI entry point from `mop-push` to `axiom-push` in pyproject.toml, create the `.github/` directory with Markdown issue templates and a PR template, and drop in the Contributor Covenant CODE_OF_CONDUCT.md.

Purpose: Establishes Axiom's GitHub community health files so contributors opening issues or PRs get the correct templates immediately. The CLI rename makes the user-facing command match the Axiom brand.

Output: pyproject.toml with `axiom-push` entry point, `.github/ISSUE_TEMPLATE/bug_report.md`, `.github/ISSUE_TEMPLATE/feature_request.md`, `.github/pull_request_template.md`, `CODE_OF_CONDUCT.md`
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-axiom-branding-community-foundation/26-CONTEXT.md

<interfaces>
<!-- Locked decisions from 26-CONTEXT.md -->
<!-- CLI rename: mop-push → axiom-push (entry point label only, mop_sdk module NOT renamed) -->
<!-- GitHub templates: Markdown format (not YAML forms) — lower friction for early-stage project -->
<!-- CODE_OF_CONDUCT.md: Contributor Covenant v2.1 (Claude's discretion to include) -->

From pyproject.toml (current state):
```toml
[project.scripts]
mop-push = "mop_sdk.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["mop_sdk*"]
```

GitHub ISSUE_TEMPLATE front matter pattern (from research):
```markdown
---
name: Bug Report
about: Report a reproducible bug in Axiom
title: "[Bug]: "
labels: bug
assignees: ''
---
```

PR template checklist must include:
- Tests pass (pytest + vitest)
- Linting passes
- New DB columns include a migration file
- No changes to /ee directory (EE boundary)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename CLI entry point in pyproject.toml</name>
  <files>pyproject.toml</files>
  <action>
In `pyproject.toml`, change the `[project.scripts]` entry from:
```toml
mop-push = "mop_sdk.cli:main"
```
to:
```toml
axiom-push = "mop_sdk.cli:main"
```

Do NOT change:
- The package name (`axiom-sdk`) — already correct
- The `include = ["mop_sdk*"]` line — mop_sdk module is NOT renamed in this phase
- Any other field in pyproject.toml

Per research: the entry point label is user-facing; the module path is internal and stays unchanged.
  </action>
  <verify>
    <automated>grep 'axiom-push = "mop_sdk.cli:main"' /home/thomas/Development/master_of_puppets/pyproject.toml && ! grep '"mop-push"' /home/thomas/Development/master_of_puppets/pyproject.toml && echo "PASS"</automated>
  </verify>
  <done>pyproject.toml has `axiom-push = "mop_sdk.cli:main"` and no remaining `mop-push` scripts entry</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub community health files</name>
  <files>
    .github/ISSUE_TEMPLATE/bug_report.md,
    .github/ISSUE_TEMPLATE/feature_request.md,
    .github/pull_request_template.md,
    CODE_OF_CONDUCT.md
  </files>
  <action>
Create the `.github/` directory structure and the following files:

**`.github/ISSUE_TEMPLATE/bug_report.md`** — Bug report template:
```markdown
---
name: Bug Report
about: Report a reproducible bug in Axiom
title: "[Bug]: "
labels: bug
assignees: ''
---

**Describe the bug**

A clear and concise description of what the bug is.

**Steps to reproduce**

1. Go to '...'
2. Run '...'
3. See error

**Expected behaviour**

What you expected to happen.

**Actual behaviour**

What actually happened. Include any error messages verbatim.

**Environment**

- Axiom version: <!-- e.g., 1.0.0-alpha -->
- Deployment type: <!-- Docker Compose / bare-metal -->
- OS: <!-- e.g., Ubuntu 22.04 -->
- Browser (dashboard issues): <!-- e.g., Chrome 122 -->

**Logs**

Paste relevant log output here (redact any secrets before posting).

```
<paste logs here>
```
```

**`.github/ISSUE_TEMPLATE/feature_request.md`** — Feature request template:
```markdown
---
name: Feature Request
about: Suggest a new feature or improvement for Axiom
title: "[Feature]: "
labels: enhancement
assignees: ''
---

**Problem this solves**

Describe the problem or limitation you are experiencing. Why does it matter?

**Proposed solution**

What would you like to see implemented? Be as specific as you can.

**Alternatives considered**

What other approaches did you consider? Why is your proposed solution preferable?

**Additional context**

Any other context, mockups, or references that would help evaluate this request.
```

**`.github/pull_request_template.md`** — PR template:
```markdown
## Summary

<!-- What does this PR do? Be concise — one paragraph or a short bullet list. -->

## Type of change

- [ ] Bug fix
- [ ] New feature
- [ ] Documentation update
- [ ] Refactor / internal improvement
- [ ] Other (describe below)

## Checklist

- [ ] Tests pass locally (`cd puppeteer && pytest` and `cd puppeteer/dashboard && npm run test -- --run`)
- [ ] Linting passes (`cd puppeteer/dashboard && npm run lint`)
- [ ] New database columns include a migration SQL file (`migration_vN.sql`)
- [ ] No changes to the `/ee` directory (community contributions to Enterprise Edition are not accepted)
- [ ] PR title follows the format: `type(scope): description` (e.g., `fix(jobs): handle null node assignment`)

## Related issues

<!-- Link any related issues: "Closes #123" or "Related to #456" -->
```

**`CODE_OF_CONDUCT.md`** — Contributor Covenant v2.1 (standard text):
Use the standard Contributor Covenant v2.1 text. Include the enforcement contact email as `[INSERT CONTACT METHOD]` placeholder — the repo owner will fill this in before making the repo public. The Contributor Covenant v2.1 full text is well-known; use the canonical wording from contributor-covenant.org version 2.1.

Key sections of v2.1: Our Pledge, Our Standards (acceptable/unacceptable behaviour), Enforcement Responsibilities, Scope, Enforcement, Enforcement Guidelines (4 levels: Correction / Warning / Temporary Ban / Permanent Ban), Attribution.
  </action>
  <verify>
    <automated>test -f /home/thomas/Development/master_of_puppets/.github/ISSUE_TEMPLATE/bug_report.md && test -f /home/thomas/Development/master_of_puppets/.github/ISSUE_TEMPLATE/feature_request.md && test -f /home/thomas/Development/master_of_puppets/.github/pull_request_template.md && test -f /home/thomas/Development/master_of_puppets/CODE_OF_CONDUCT.md && grep -q "Contributor Covenant" /home/thomas/Development/master_of_puppets/CODE_OF_CONDUCT.md && grep -q "No changes to" /home/thomas/Development/master_of_puppets/.github/pull_request_template.md && echo "PASS"</automated>
  </verify>
  <done>All four files exist with correct content. Bug report has `labels: bug` front matter. PR template has /ee boundary check. CODE_OF_CONDUCT uses Contributor Covenant v2.1 text.</done>
</task>

</tasks>

<verification>
All files created correctly:
- `grep 'axiom-push' pyproject.toml` — returns match
- `grep 'mop-push' pyproject.toml` — returns no match
- All four GitHub/community files exist at correct paths
- PR template includes `/ee` boundary check line
- CODE_OF_CONDUCT mentions Contributor Covenant
</verification>

<success_criteria>
- pyproject.toml: `axiom-push = "mop_sdk.cli:main"` is the only scripts entry, no `mop-push` remains
- `.github/ISSUE_TEMPLATE/bug_report.md` exists with `labels: bug` front matter and structured sections
- `.github/ISSUE_TEMPLATE/feature_request.md` exists with `labels: enhancement` front matter
- `.github/pull_request_template.md` exists with test checklist and /ee boundary check
- `CODE_OF_CONDUCT.md` exists with Contributor Covenant v2.1 full text
</success_criteria>

<output>
After completion, create `.planning/phases/26-axiom-branding-community-foundation/26-01-SUMMARY.md`
</output>
