---
phase: 10-windows-installer-fix
plan: 03
type: execute
wave: 2
depends_on:
  - 10-02
files_modified: []
autonomous: false
requirements:
  - WIN-06
  - WIN-07

must_haves:
  truths:
    - "loader/Containerfile builds successfully into a runnable container image"
    - "All Pester tests pass in a clean run (final green gate)"
    - "Human has verified Method 1 works end-to-end on Windows or WSL2"
  artifacts:
    - path: "puppeteer/installer/loader/Containerfile"
      provides: "Loader container that builds successfully with podman build"
  key_links:
    - from: "puppeteer/installer/install_universal.ps1"
      to: "puppeteer/installer/loader/Containerfile"
      via: "podman build -t puppeteer-loader -f loader/Containerfile ."
      pattern: "loader/Containerfile"
---

<objective>
Verify Phase 10 is complete: smoke-test the loader/Containerfile build and confirm the human-in-the-loop end-to-end check (WIN-07) on Windows or WSL2.

Purpose: WIN-06 and WIN-07 cannot be automated in CI — they require a local Podman installation. This plan ensures they are checked before the phase is marked complete.

Output: Phase 10 gate passed — all requirements WIN-01 through WIN-07 verified.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-windows-installer-fix/10-02-SUMMARY.md
@puppeteer/installer/install_universal.ps1
@puppeteer/installer/loader/Containerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full Pester suite — final green gate</name>
  <files></files>
  <action>
Run the full Pester test suite to confirm all tests from Plans 01 and 02 are GREEN. This is the automated final gate before the manual checkpoint.

Command to run:
```powershell
pwsh -Command "Invoke-Pester /home/thomas/Development/master_of_puppets/puppeteer/installer/tests/ -Output Detailed"
```

If any test fails:
- Read the failure output
- Check whether the failure is in the test itself (assertion wording) or in the implementation
- Fix in `install_universal.ps1` or `installer.Tests.ps1` as appropriate
- Re-run until all GREEN

Do NOT proceed to the checkpoint task if any test is RED.
  </action>
  <verify>
    <automated>pwsh -Command "Invoke-Pester /home/thomas/Development/master_of_puppets/puppeteer/installer/tests/ -Output Minimal; if (\$LASTEXITCODE -ne 0) { exit 1 }"</automated>
  </verify>
  <done>Pester exits 0. Output shows all tests passed, 0 failed, 0 skipped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify WIN-06 (Containerfile build) and WIN-07 (end-to-end on Windows/WSL2)</name>
  <files>puppeteer/installer/loader/Containerfile</files>
  <action>Human performs WIN-06 smoke build and WIN-07 end-to-end verification as described in how-to-verify below.</action>
  <verify>
    <automated>MISSING — WIN-06 and WIN-07 require Podman installed locally; human verification required</automated>
  </verify>
  <done>WIN-06: podman build exits 0. WIN-07: Method 1 reaches loader container launch without named-pipe mount error on Windows or WSL2.</done>
  <what-built>
    Fixed `install_universal.ps1` Method 1 (Loader) path:
    - WIN-01: `Assert-PodmanMachineRunning` guard before loader launch
    - WIN-02: `Get-PodmanSocketInfo` using `PodmanPipe.Path` (not PodmanSocket)
    - WIN-03: TCP relay via `podman system service` for native Windows; unix socket bind for WSL/Linux
    - WIN-04: `Invoke-Expression` replaced with `& podman @LoaderArgs` splatting
    - WIN-05: `podman-compose` check moved to Method-2 else branch only
    - WIN-06: `loader/Containerfile` (Fedora 40, installs podman + podman-compose + python3)
  </what-built>
  <how-to-verify>
    **WIN-06: Containerfile smoke build (requires Podman installed locally)**
    ```bash
    cd puppeteer/installer/loader
    podman build -t mop-loader-test .
    # Expected: build completes successfully, image tagged as mop-loader-test
    # If build fails: note the error output and report
    ```

    **WIN-07: Method 1 end-to-end (requires Windows or WSL2 with Podman machine)**

    On a Windows machine with Podman Desktop installed, or in WSL2 with a Podman machine configured:

    1. Ensure a Podman machine is running: `podman machine list` — at least one entry shows `Running = true`
    2. Navigate to the `puppeteer/installer/` directory
    3. Run: `pwsh ./install_universal.ps1 -Role Agent`
    4. When prompted for platform, select Podman
    5. When prompted for method, select `[1] Automatic (Loader Container)`
    6. Expected sequence:
       - "Checking Podman machine..." — no error
       - "Building Loader Image..." — `podman build` completes
       - On native Windows: "Starting Podman TCP relay..." then `podman run` with `DOCKER_HOST`
       - On WSL2: `podman run` with socket bind mount
       - Loader container starts and begins `podman-compose -f compose.server.yaml up -d`
    7. Note: You do NOT need a full MOP stack for this test — observing that the loader container starts without a socket/pipe error is sufficient

    If you don't have Windows available, test WSL2 path:
    ```powershell
    # In WSL2 with pwsh installed:
    cd /path/to/master_of_puppets/puppeteer/installer
    pwsh ./install_universal.ps1 -Role Agent -Platform Podman
    # Select method 1
    ```

    **Minimum pass criteria:**
    - WIN-06: `podman build` exits 0 for the Containerfile
    - WIN-07 pass: Loader container starts (no named-pipe mount error); OR if Windows hardware unavailable, confirm WSL2 path reaches `podman run` without Invoke-Expression error
    - WIN-07 skip: If neither Windows nor WSL2+Podman is available, type "skip-win07" with explanation
  </how-to-verify>
  <resume-signal>Type "approved" if both WIN-06 and WIN-07 passed. Type "approved-win06-only" if Containerfile builds but WIN-07 hardware not available. Type "issues: [description]" if something failed.</resume-signal>
</task>

</tasks>

<verification>
Phase complete when:
1. Pester full suite exits 0 (all WIN-01..05 tests GREEN)
2. WIN-06: `podman build -t mop-loader-test puppeteer/installer/loader/` exits 0
3. WIN-07: Human-verified Method 1 path reaches loader container launch without error
4. All requirement IDs WIN-01 through WIN-07 have a verifying artifact or human sign-off
</verification>

<success_criteria>
- All 5 Pester tests GREEN (WIN-01 through WIN-05 automated)
- loader/Containerfile builds successfully (WIN-06 smoke)
- Method 1 works on Windows or WSL2 without named-pipe mount error (WIN-07 human)
- Phase 10 can be marked Complete in ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/10-windows-installer-fix/10-03-SUMMARY.md`

Then update `.planning/ROADMAP.md` Phase 10 status to Complete with today's date.
Then update `.planning/STATE.md` to reflect Phase 10 complete.
</output>
