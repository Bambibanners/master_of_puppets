---
phase: 04-rollout-ui
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - puppeteer/dashboard/src/components/HotUpgradeModal.tsx
  - puppeteer/dashboard/src/views/Nodes.tsx
autonomous: true
requirements:
  - RUI-02 (Safety Gates)

must_haves:
  truths:
    - "Upgrade process requires a multi-step confirmation"
    - "Admins must type 'UPGRADE' to execute"
  artifacts:
    - path: "puppeteer/dashboard/src/components/HotUpgradeModal.tsx"
      provides: "Gated upgrade workflow"
      contains: "const [gate, setGate] ="
---

<objective>
Build a secure, gated workflow for pushing runtime upgrades to individual nodes.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/ROADMAP.md
@/home/thomas/Development/master_of_puppets/.planning/phases/04-rollout-ui/04-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create HotUpgradeModal component</name>
  <files>puppeteer/dashboard/src/components/HotUpgradeModal.tsx</files>
  <action>
    Create `puppeteer/dashboard/src/components/HotUpgradeModal.tsx`.
    1. **Fetch Tools**: Load available tools from `/api/capability-matrix`.
    2. **Gate 1**: Summary of tool to be installed.
    3. **Gate 2**: Raw recipe preview.
    4. **Gate 3**: Text input confirmation ("UPGRADE").
    5. **Execution**: Fire `POST /api/nodes/{id}/upgrade` on completion.
  </action>
  <verify>
    <automated>ls puppeteer/dashboard/src/components/HotUpgradeModal.tsx && echo "File OK"</automated>
  </verify>
  <done>Hot-upgrade modal with safety gates is implemented</done>
</task>

<task type="auto">
  <name>Task 2: Wire Modal to NodeCard</name>
  <files>puppeteer/dashboard/src/views/Nodes.tsx</files>
  <action>
    Add a "Push Tool" button to the `NodeCard` (near other management actions).
    Open `HotUpgradeModal` when clicked.
  </action>
  <verify>
    <automated>grep -q "HotUpgradeModal" puppeteer/dashboard/src/views/Nodes.tsx && echo "Wired OK"</automated>
  </verify>
  <done>Operators can trigger upgrades directly from the node card</done>
</task>

</tasks>

<verification>
1. Open HotUpgradeModal for a node.
2. Verify tool selection filters by node OS (if possible).
3. Walk through all gates. Confirm "Apply" button is disabled until "UPGRADE" is typed.
</verification>

<success_criteria>
- Accidental upgrades are prevented by mandatory safety checks.
- Operators have full visibility into the changes being applied.
</success_criteria>

<output>
After completion, create `.planning/phases/04-rollout-ui/04-02-SUMMARY.md`
</output>
