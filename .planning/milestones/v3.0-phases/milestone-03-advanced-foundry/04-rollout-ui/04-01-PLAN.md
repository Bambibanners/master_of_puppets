---
phase: 04-rollout-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/dashboard/src/views/Nodes.tsx
autonomous: true
requirements:
  - RUI-01 (Drift Visualization)

must_haves:
  truths:
    - "NodeCard displays a comparison between reported and expected capabilities"
    - "Drifted states (missing/mismatched tools) are visually distinct"
  artifacts:
    - path: "puppeteer/dashboard/src/views/Nodes.tsx"
      provides: "Runtime health UI"
      contains: "const checkDrift ="
---

<objective>
Implement the visual logic to surface capability drift and authorized runtime status in the Node dashboard.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/ROADMAP.md
@/home/thomas/Development/master_of_puppets/.planning/phases/04-rollout-ui/04-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add drift detection logic to NodeCard</name>
  <files>puppeteer/dashboard/src/views/Nodes.tsx</files>
  <behavior>
    - Create a helper to calculate drift:
        - Input: `node.capabilities`, `node.expected_capabilities`.
        - Output: List of tools with status: `AUTHORIZED`, `DRIFTED_MISSING`, `DRIFTED_VERSION`, `UNAUTHORIZED` (Tampered).
  </behavior>
  <action>
    Update `NodeCard` in `Nodes.tsx` to compute the runtime health state.
  </action>
  <verify>
    <automated>grep -q "expected_capabilities" puppeteer/dashboard/src/views/Nodes.tsx && echo "Logic OK"</automated>
  </verify>
  <done>Capability comparison engine is implemented in the frontend</done>
</task>

<task type="auto">
  <name>Task 2: Implement Runtime Health UI section</name>
  <files>puppeteer/dashboard/src/views/Nodes.tsx</files>
  <action>
    Modify `NodeCard` rendering:
    1. Add a collapsible "Runtime Health" section.
    2. Render the comparison list with badges:
        - `In Compliance` (Green check)
        - `Drifted` (Amber warning)
        - `Tampered` (Red alert)
  </action>
  <verify>
    <automated>grep -q "Runtime Health" puppeteer/dashboard/src/views/Nodes.tsx && echo "UI Section OK"</automated>
  </verify>
  <done>Operators can visually identify which nodes need upgrades</done>
</task>

</tasks>

<verification>
1. Load dashboard. Verify authorized tools show a green indicator.
2. Manually mock a mismatched capability in the state and verify the amber "Drifted" badge appears.
</verification>

<success_criteria>
- Zero-Trust model is visible to operators.
- Authorized vs Reported tools are clearly differentiated.
</success_criteria>

<output>
After completion, create `.planning/phases/04-rollout-ui/04-01-SUMMARY.md`
</output>
