---
phase: 04-rollout-ui
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - puppeteer/dashboard/src/views/Admin.tsx
autonomous: true
requirements:
  - RUI-03 (Staged Rollouts)

must_haves:
  truths:
    - "Rollouts tab exists in Admin view"
    - "Supports multi-node selection for batch upgrades"
  artifacts:
    - path: "puppeteer/dashboard/src/views/Admin.tsx"
      provides: "Bulk operation dashboard"
      contains: "const RolloutManager ="
---

<objective>
Provide a centralized dashboard for managing runtime upgrades across multiple nodes simultaneously.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/ROADMAP.md
@/home/thomas/Development/master_of_puppets/.planning/phases/04-rollout-ui/04-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Rollouts tab to Admin view</name>
  <files>puppeteer/dashboard/src/views/Admin.tsx</files>
  <action>
    Add a new `TabsTrigger` and `TabsContent` for "Rollouts".
    Implement `RolloutManager` component:
    1. **Selection**: Choose a Tool from the `CapabilityMatrix`.
    2. **Filtering**: Select Nodes by tag (e.g. `env:dev`) or status.
    3. **Batch Trigger**: Iterate through selected nodes and call the `/upgrade` API.
    4. **Progress**: Show a summary of "Staged / Succeeded / Failed" for the batch.
  </action>
  <verify>
    <automated>grep -q "value="rollouts"" puppeteer/dashboard/src/views/Admin.tsx && echo "Tab OK"</automated>
  </verify>
  <done>Centralized rollout management is functional</done>
</task>

</tasks>

<verification>
1. Navigate to Admin -> Rollouts.
2. Select a tool and two target nodes.
3. Confirm the batch action correctly stages upgrades for both.
</verification>

<success_criteria>
- Administrators can perform staged rollouts of runtime updates.
- Fleet-wide state management is more efficient than per-node actions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-rollout-ui/04-03-SUMMARY.md`
</output>
