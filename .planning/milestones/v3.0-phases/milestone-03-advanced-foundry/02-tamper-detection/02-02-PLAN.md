---
phase: 02-tamper-detection
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - puppeteer/agent_service/main.py
autonomous: true
requirements:
  - TDA-02 (Expectation Initialization)

must_haves:
  truths:
    - "admin/generate-token accepts and saves template_id"
    - "api/enroll initializes Node.expected_capabilities from template blueprints"
  artifacts:
    - path: "puppeteer/agent_service/main.py"
      provides: "Automated expectation derivation"
      contains: "expected_caps = {tool['id']"
---

<objective>
Automate the initialization of authorized node capabilities during the enrollment process based on the assigned template.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/ROADMAP.md
@/home/thomas/Development/master_of_puppets/.planning/phases/02-tamper-detection/02-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update token generation to support template_id</name>
  <files>puppeteer/agent_service/main.py</files>
  <action>
    Modify `create_enrollment_token` in `main.py`:
    1. Accept an optional `template_id` in the request.
    2. Save it to the `Token` record.
  </action>
  <verify>
    <automated>grep -q "token_entry.template_id =" puppeteer/agent_service/main.py && echo "Token Logic OK"</automated>
  </verify>
  <done>Tokens can now be linked to specific PuppetTemplates</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Initialize node expectations during enrollment</name>
  <files>puppeteer/agent_service/main.py</files>
  <behavior>
    - During `enroll_node`:
        - If `token_entry.template_id` is set:
            - Load the `PuppetTemplate`.
            - Load its `runtime_blueprint`.
            - Extract `tools` from the blueprint definition.
            - Format as JSON: `{"tool_id": "latest", ...}`.
            - Set `node.expected_capabilities`.
  </behavior>
  <action>
    Update `enroll_node` logic in `main.py` to calculate and store the expected capabilities map.
  </action>
  <verify>
    <automated>grep -q "node.expected_capabilities = json.dumps" puppeteer/agent_service/main.py && echo "Enroll Logic OK"</automated>
  </verify>
  <done>Newly enrolled nodes start with an authorized capability baseline</done>
</task>

</tasks>

<verification>
1. Generate a token with a template ID.
2. Enroll a node with that token.
3. Verify `node.expected_capabilities` in the DB matches the template's tool list.
</verification>

<success_criteria>
- Enrollment tokens correctly propagate template affinity.
- Authorized capabilities are automatically calculated and locked in during node creation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-tamper-detection/02-02-SUMMARY.md`
</output>
