---
phase: 02-tamper-detection
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - puppeteer/agent_service/services/job_service.py
autonomous: true
requirements:
  - TDA-03 (Comparison Guard)
  - TDA-04 (Quarantine)

must_haves:
  truths:
    - "receive_heartbeat flags nodes as TAMPERED if unauthorized capabilities are reported"
    - "pull_work rejects nodes with TAMPERED status"
  artifacts:
    - path: "puppeteer/agent_service/services/job_service.py"
      provides: "Runtime security enforcement"
      contains: "node.status = "TAMPERED""
---

<objective>
Implement the core security logic to detect capability tampering during heartbeats and quarantine compromised nodes.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/ROADMAP.md
@/home/thomas/Development/master_of_puppets/.planning/phases/02-tamper-detection/02-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Heartbeat Comparison Logic</name>
  <files>puppeteer/agent_service/services/job_service.py</files>
  <behavior>
    - In `receive_heartbeat`:
        - If `node.expected_capabilities` is present:
            - Parse `expected_capabilities` (JSON).
            - Check `hb.capabilities` keys against authorized keys.
            - If a key exists in `Reported` but NOT in `Expected` -> **TAMPER DETECTED**.
            - Transition `node.status = "TAMPERED"`.
            - Set `node.tamper_details` with the offending tool name.
  </behavior>
  <action>
    Update `receive_heartbeat` in `puppeteer/agent_service/services/job_service.py`.
  </action>
  <verify>
    <automated>grep -q "node.status = "TAMPERED"" puppeteer/agent_service/services/job_service.py && echo "Guard Logic OK"</automated>
  </verify>
  <done>Heartbeat processing now performs Zero-Trust validation</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce Quarantine in Job Dispatch</name>
  <files>puppeteer/agent_service/services/job_service.py</files>
  <behavior>
    - In `pull_work`:
        - Add a check at the beginning of the function.
        - If `node.status == "TAMPERED"`, immediately return `PollResponse(job=None)` with a security warning in the logs.
  </behavior>
  <action>
    Update `pull_work` in `puppeteer/agent_service/services/job_service.py`.
  </action>
  <verify>
    <automated>grep -q "if node.status == "TAMPERED":" puppeteer/agent_service/services/job_service.py && echo "Quarantine OK"</automated>
  </verify>
  <done>Tampered nodes are blocked from receiving work</done>
</task>

</tasks>

<verification>
1. Set `expected_capabilities` for a node to `{"python": "latest"}`.
2. Send heartbeat from that node with `{"python": "3.11", "malware": "1.0"}`.
3. Verify node status becomes `TAMPERED`.
4. Try to pull work for that node; verify no jobs are returned.
</verification>

<success_criteria>
- Unauthorized runtime modifications are detected within one heartbeat cycle.
- Detection automatically results in isolation from the job queue.
</success_criteria>

<output>
After completion, create `.planning/phases/02-tamper-detection/02-03-SUMMARY.md`
</output>
