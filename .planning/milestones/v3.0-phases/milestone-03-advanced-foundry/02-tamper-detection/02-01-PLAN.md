---
phase: 02-tamper-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/agent_service/db.py
  - puppeteer/agent_service/models.py
  - puppeteer/migration_v20.sql
autonomous: true
requirements:
  - TDA-01 (Persistence)

must_haves:
  truths:
    - "Node ORM has expected_capabilities and tamper_details"
    - "Token ORM has template_id"
    - "migration_v20.sql contains idempotent ALTER TABLE statements"
  artifacts:
    - path: "puppeteer/agent_service/db.py"
      provides: "Security metadata for nodes"
      contains: "expected_capabilities"
---

<objective>
Update the database schema to track authorized capabilities and link enrollment tokens to templates.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/ROADMAP.md
@/home/thomas/Development/master_of_puppets/.planning/phases/02-tamper-detection/02-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add security fields to Node and Token in db.py</name>
  <files>puppeteer/agent_service/db.py</files>
  <action>
    Modify `db.py`:
    1. Update `Node`: Add `expected_capabilities` (Text, nullable) and `tamper_details` (Text, nullable).
    2. Update `Token`: Add `template_id` (String, nullable).
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && /home/thomas/Development/master_of_puppets/.venv/bin/python -c "from agent_service.db import Node, Token; assert hasattr(Node, 'expected_capabilities'); assert hasattr(Token, 'template_id'); print('Models OK')"</automated>
  </verify>
  <done>Database models are updated</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update Pydantic models in models.py</name>
  <files>puppeteer/agent_service/models.py</files>
  <action>
    Update `models.py`:
    1. Update `NodeResponse` to include `expected_capabilities` and `tamper_details`.
    2. Update `TokenResponse` (if exists) or the token generation payload to include `template_id`.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && /home/thomas/Development/master_of_puppets/.venv/bin/python -c "from agent_service.models import NodeResponse; print('Models OK')"</automated>
  </verify>
  <done>Data contracts are updated</done>
</task>

<task type="auto">
  <name>Task 3: Create migration_v20.sql</name>
  <files>puppeteer/migration_v20.sql</files>
  <action>
    Create `puppeteer/migration_v20.sql` with idempotent SQL for Postgres.
  </action>
  <verify>
    <automated>grep -q "expected_capabilities" puppeteer/migration_v20.sql && echo "Migration OK"</automated>
  </verify>
  <done>Postgres migration file is ready</done>
</task>

</tasks>

<verification>
1. `python -c "from agent_service.db import Node; ..."` succeeds.
2. `migration_v20.sql` contains correct `ALTER` statements.
</verification>

<success_criteria>
- Schema is ready to support Zero-Trust capability tracking.
- Tokens can now carry template affinity.
</success_criteria>

<output>
After completion, create `.planning/phases/02-tamper-detection/02-01-SUMMARY.md`
</output>
