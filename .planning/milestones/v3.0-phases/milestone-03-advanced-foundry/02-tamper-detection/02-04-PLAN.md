---
phase: 02-tamper-detection
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - puppeteer/agent_service/main.py
  - puppeteer/dashboard/src/views/Nodes.tsx
autonomous: true
requirements:
  - TDA-05 (Recovery)

must_haves:
  truths:
    - "POST /nodes/{id}/clear-tamper resets node status to ONLINE"
    - "Dashboard displays critical alert badge for TAMPERED nodes"
  artifacts:
    - path: "puppeteer/agent_service/main.py"
      provides: "Security recovery endpoint"
      contains: "async def clear_node_tamper"
---

<objective>
Provide administrators with the tools to visualize security breaches and restore nodes to service after investigation.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/ROADMAP.md
@/home/thomas/Development/master_of_puppets/.planning/phases/02-tamper-detection/02-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Clear Tamper Alert API</name>
  <files>puppeteer/agent_service/main.py</files>
  <action>
    Add a new endpoint to `main.py`:
    - `POST /api/nodes/{node_id}/clear-tamper`
    - Restricted to `admin` role.
    - Logic: Set `node.status = "ONLINE"`, clear `node.tamper_details`.
  </action>
  <verify>
    <automated>grep -q "async def clear_node_tamper" puppeteer/agent_service/main.py && echo "API OK"</automated>
  </verify>
  <done>Administrators can programmatically clear security alerts</done>
</task>

<task type="auto">
  <name>Task 2: Visualize Tamper Alerts in Dashboard</name>
  <files>puppeteer/dashboard/src/views/Nodes.tsx</files>
  <action>
    Update `Nodes.tsx`:
    1. Update status badge logic to handle `TAMPERED` (Color: Red/Alert).
    2. Display `tamper_details` in the node card if status is `TAMPERED`.
    3. Add a "Clear Alert" button to the node management actions (visible only if status is `TAMPERED`).
  </action>
  <verify>
    <automated>grep -q "TAMPERED" puppeteer/dashboard/src/views/Nodes.tsx && echo "UI Logic OK"</automated>
  </verify>
  <done>Security breaches are highly visible to operators</done>
</task>

</tasks>

<verification>
1. Mark a node as `TAMPERED` in the DB.
2. Verify it appears with a red alert badge in the dashboard.
3. Click "Clear Alert" and verify node returns to `ONLINE`.
</verification>

<success_criteria>
- Security incidents are surfaced immediately in the management console.
- A controlled recovery path exists for authorized personnel.
</success_criteria>

<output>
After completion, create `.planning/phases/02-tamper-detection/02-04-SUMMARY.md`
</output>
