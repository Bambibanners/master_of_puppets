---
phase: 01-capability-vault
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - puppeteer/agent_service/main.py
  - puppeteer/agent_service/services/foundry_service.py
autonomous: true
requirements:
  - CAV-03 (Capability CRUD)

must_haves:
  truths:
    - "CRUD endpoints for CapabilityMatrix exist"
    - "ApprovedOS list can be updated via API"
    - "Recipes support {{ARTIFACT_URL}} macro"
  artifacts:
    - path: "puppeteer/agent_service/main.py"
      provides: "New configuration endpoints"
      contains: "app.get("/api/approved-os")"
---

<objective>
Expose administrative APIs for managing pre-approved OS bases and dynamic capability recipes.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/PROJECT.md
@/home/thomas/Development/master_of_puppets/.planning/phases/01-capability-vault/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ApprovedOS CRUD in main.py</name>
  <files>puppeteer/agent_service/main.py</files>
  <action>
    Add routes to `main.py`:
    1. `GET /api/approved-os`: List all approved bases.
    2. `POST /api/approved-os`: Register new base image.
    3. `DELETE /api/approved-os/{id}`: Remove base image.
  </action>
  <verify>
    <automated>grep -q "app.get("/api/approved-os")" puppeteer/agent_service/main.py && echo "OS API OK"</automated>
  </verify>
  <done>Approved OS management is live</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand Capability Matrix API in main.py</name>
  <files>puppeteer/agent_service/main.py</files>
  <action>
    Extend existing `CapabilityMatrix` endpoints:
    1. Implement `POST /api/capability-matrix` (Create).
    2. Implement `PUT /api/capability-matrix/{id}` (Update).
    3. Implement `DELETE /api/capability-matrix/{id}` (Delete).
  </action>
  <verify>
    <automated>grep -q "app.delete("/api/capability-matrix/{id}")" puppeteer/agent_service/main.py && echo "Matrix API OK"</automated>
  </verify>
  <done>Capability Matrix is fully dynamic</done>
</task>

<task type="auto">
  <name>Task 3: Implement Macro Expansion in FoundryService</name>
  <files>puppeteer/agent_service/services/foundry_service.py</files>
  <action>
    Modify `FoundryService.build_template`:
    - Before writing `injection_recipe` to Dockerfile, replace `{{ARTIFACT_URL}}` with the actual internal vault URL if `artifact_id` is present.
  </action>
  <verify>
    <automated>grep -q "replace("{{ARTIFACT_URL}}"" puppeteer/agent_service/services/foundry_service.py && echo "Macro OK"</automated>
  </verify>
  <done>Recipes can reference local artifacts</done>
</task>

</tasks>

<verification>
1. `curl -X GET .../api/approved-os` returns list.
2. `FoundryService` correctly replaces macros during build (code review).
</verification>

<success_criteria>
- Capabilities are no longer static; they can be tuned by admins.
- Built images can pull artifacts from the internal vault during the build phase.
</success_criteria>

<output>
After completion, create `.planning/phases/01-capability-vault/01-03-SUMMARY.md`
</output>
