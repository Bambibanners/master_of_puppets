---
phase: 01-capability-vault
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - puppeteer/dashboard/src/views/Admin.tsx
  - puppeteer/dashboard/src/components/CreateBlueprintDialog.tsx
autonomous: true
requirements:
  - CAV-04 (Admin UI)

must_haves:
  truths:
    - "Admin dashboard has a Matrix & Artifacts management view"
    - "Operators can upload new artifacts from the UI"
    - "CreateBlueprintDialog uses the dynamic ApprovedOS list"
  artifacts:
    - path: "puppeteer/dashboard/src/views/Admin.tsx"
      provides: "New management interface"
      contains: "<TabsContent value="matrix">"
---

<objective>
Build the operator interface for managing the dynamic capability registry and binary artifact vault.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/PROJECT.md
@/home/thomas/Development/master_of_puppets/.planning/phases/01-capability-vault/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Matrix & Artifacts tabs to Admin view</name>
  <files>puppeteer/dashboard/src/views/Admin.tsx</files>
  <action>
    Update `Admin.tsx` to include two new tabs:
    1. **Matrix**: A table showing `base_os_family`, `tool_id`, and `injection_recipe`. Include "Edit" and "Delete" buttons. Add a "Register Tool" button that opens a form.
    2. **Vault**: A list of uploaded artifacts with filenames, sizes, and hashes. Include an "Upload Artifact" button that uses a file input.
  </action>
  <verify>
    <automated>grep -q "matrix" puppeteer/dashboard/src/views/Admin.tsx && echo "UI Tabs OK"</automated>
  </verify>
  <done>Admin UI is expanded</done>
</task>

<task type="auto">
  <name>Task 2: Wire Dynamic OS List to Blueprint Dialog</name>
  <files>puppeteer/dashboard/src/components/CreateBlueprintDialog.tsx</files>
  <action>
    Modify `CreateBlueprintDialog.tsx`:
    - Instead of hardcoded OS options, fetch the list from `GET /api/approved-os` on mount.
    - Populate the `base_os` select dropdown with these values.
  </action>
  <verify>
    <automated>grep -q "approved-os" puppeteer/dashboard/src/components/CreateBlueprintDialog.tsx && echo "UI Dynamic OK"</automated>
  </verify>
  <done>Blueprints use the official approved OS list</done>
</task>

</tasks>

<verification>
1. Navigate to /admin. Verify new tabs appear.
2. Open Create Blueprint. Verify OS list is fetched from API.
</verification>

<success_criteria>
- Operators have a visual way to manage the platform's tool registry.
- Provisioning of new blueprints is constrained by the approved OS list.
</success_criteria>

<output>
After completion, create `.planning/phases/01-capability-vault/01-04-SUMMARY.md`
</output>
