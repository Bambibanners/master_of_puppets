---
phase: 01-capability-vault
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/agent_service/db.py
  - puppeteer/agent_service/models.py
  - puppeteer/migration_v19.sql
autonomous: true
requirements:
  - CAV-01 (Data Model)

must_haves:
  truths:
    - "Artifact ORM class exists with SHA256 and size_bytes"
    - "ApprovedOS ORM class exists for base image tracking"
    - "CapabilityMatrix ORM updated with artifact_id foreign key"
  artifacts:
    - path: "puppeteer/agent_service/db.py"
      provides: "New persistence models"
      contains: "class Artifact"
    - path: "puppeteer/migration_v19.sql"
      provides: "Idempotent schema update"
      contains: "CREATE TABLE IF NOT EXISTS artifacts"
---

<objective>
Establish the database schema for artifact hosting and dynamic capability management.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/PROJECT.md
@/home/thomas/Development/master_of_puppets/.planning/phases/01-capability-vault/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Artifact and ApprovedOS models to db.py</name>
  <files>puppeteer/agent_service/db.py</files>
  <action>
    Modify `puppeteer/agent_service/db.py`. 
    1. Add `Artifact` table: `id` (String UUID), `filename`, `content_type`, `sha256`, `size_bytes`, `created_at`.
    2. Add `ApprovedOS` table: `id` (Integer), `name` (e.g. Debian 12), `image_uri` (e.g. debian:12-slim), `os_family` (DEBIAN/ALPINE).
    3. Update `CapabilityMatrix`: Add `artifact_id: Mapped[Optional[str]] = mapped_column(String, ForeignKey("artifacts.id"), nullable=True)`.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && /home/thomas/Development/master_of_puppets/.venv/bin/python -c "from agent_service.db import Artifact, ApprovedOS, CapabilityMatrix; print('Models OK')"</automated>
  </verify>
  <done>Database models are defined</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define Pydantic models in models.py</name>
  <files>puppeteer/agent_service/models.py</files>
  <action>
    Update `puppeteer/agent_service/models.py`.
    1. Add `ArtifactResponse(BaseModel)`.
    2. Add `ApprovedOSResponse(BaseModel)`.
    3. Update `CapabilityMatrixEntry` to include `artifact_id`.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && /home/thomas/Development/master_of_puppets/.venv/bin/python -c "from agent_service.models import ArtifactResponse; print('Models OK')"</automated>
  </verify>
  <done>Data contracts are updated</done>
</task>

<task type="auto">
  <name>Task 3: Create migration_v19.sql</name>
  <files>puppeteer/migration_v19.sql</files>
  <action>
    Create `puppeteer/migration_v19.sql` with idempotent SQL for Postgres.
  </action>
  <verify>
    <automated>grep -q "artifacts" puppeteer/migration_v19.sql && echo "Migration OK"</automated>
  </verify>
  <done>Postgres migration file is ready</done>
</task>

</tasks>

<verification>
1. `python -c "from agent_service.db import Artifact; ..."` succeeds.
2. `migration_v19.sql` contains `CREATE TABLE IF NOT EXISTS artifacts`.
</verification>

<success_criteria>
- System is ready to persist artifact metadata and approved OS lists.
- Capability Matrix is correctly linked to the Artifact table.
</success_criteria>

<output>
After completion, create `.planning/phases/01-capability-vault/01-01-SUMMARY.md`
</output>
