---
phase: 01-capability-vault
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - puppeteer/agent_service/main.py
  - puppeteer/agent_service/services/vault_service.py
autonomous: true
requirements:
  - CAV-02 (Artifact API)

must_haves:
  truths:
    - "POST /api/artifacts handles multipart file uploads"
    - "GET /api/artifacts/{id}/download returns a StreamingResponse"
    - "SHA256 checksum is calculated during upload"
  artifacts:
    - path: "puppeteer/agent_service/services/vault_service.py"
      provides: "File storage orchestration"
      contains: "async def store_artifact"
---

<objective>
Implement the Artifact Vault service and API for secure binary storage and retrieval.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/PROJECT.md
@/home/thomas/Development/master_of_puppets/.planning/phases/01-capability-vault/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create VaultService for file management</name>
  <files>puppeteer/agent_service/services/vault_service.py</files>
  <action>
    Create `puppeteer/agent_service/services/vault_service.py`.
    1. Implement `store_artifact(file: UploadFile, db: AsyncSession)`:
        - Generate UUID.
        - Stream file to `/app/vault/{uuid}`.
        - Calculate SHA256 on the fly.
        - Create `Artifact` DB record.
    2. Implement `get_artifact_path(artifact_id: str, db: AsyncSession)`:
        - Return absolute path if exists.
    3. Implement `delete_artifact(artifact_id: str, db: AsyncSession)`:
        - Remove file from disk.
        - Remove record from DB.
  </action>
  <verify>
    <automated>ls puppeteer/agent_service/services/vault_service.py && echo "Service OK"</automated>
  </verify>
  <done>Vault service is implemented</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Artifact API endpoints in main.py</name>
  <files>puppeteer/agent_service/main.py</files>
  <action>
    Add routes to `puppeteer/agent_service/main.py`:
    1. `POST /api/artifacts`: Protected by `foundry:write`.
    2. `GET /api/artifacts`: List all artifacts.
    3. `GET /api/artifacts/{id}/download`: Binary stream download.
    4. `DELETE /api/artifacts/{id}`: Protected by `foundry:write`.
  </action>
  <verify>
    <automated>grep -q "app.post("/api/artifacts")" puppeteer/agent_service/main.py && echo "API OK"</automated>
  </verify>
  <done>Artifact API is live</done>
</task>

</tasks>

<verification>
1. `grep "StreamingResponse" puppeteer/agent_service/main.py` verified.
2. `ls puppeteer/agent_service/services/vault_service.py` verified.
</verification>

<success_criteria>
- Files can be uploaded to the server and persisted on disk.
- Artifacts are tracked in the database with integrity hashes.
- Files can be downloaded via authenticated API.
</success_criteria>

<output>
After completion, create `.planning/phases/01-capability-vault/01-02-SUMMARY.md`
</output>
