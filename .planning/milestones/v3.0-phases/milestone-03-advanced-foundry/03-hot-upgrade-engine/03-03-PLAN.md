---
phase: 03-hot-upgrade-engine
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - puppeteer/agent_service/services/job_service.py
  - puppets/environment_service/node.py
autonomous: true
requirements:
  - HUE-04 (C2 Loop)
  - HUE-05 (Promotion)

must_haves:
  truths:
    - "Heartbeat response delivers upgrade_task if pending"
    - "Orchestrator promotes pending to expected_capabilities on success"
    - "Node reports upgrade result in next heartbeat"
  artifacts:
    - path: "puppeteer/agent_service/services/job_service.py"
      provides: "Heartbeat orchestration"
      contains: "resp["upgrade_task"] ="
---

<objective>
Close the command-and-control loop by integrating hot-upgrades into the heartbeat traffic and implementing capability promotion.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/ROADMAP.md
@/home/thomas/Development/master_of_puppets/.planning/phases/03-hot-upgrade-engine/03-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Node.heartbeat_loop to handle tasks</name>
  <files>puppets/environment_service/node.py</files>
  <action>
    Modify `heartbeat_loop` in `node.py`:
    1. Parse the JSON response from the heartbeat POST.
    2. If `upgrade_task` is present, call `self.execute_upgrade(task)`.
    3. Store the result in a local state.
    4. Include `upgrade_result` in the NEXT heartbeat payload.
  </action>
  <verify>
    <automated>grep -q "upgrade_task" puppets/environment_service/node.py && echo "Node C2 OK"</automated>
  </verify>
  <done>Puppet agents respond to instructions in heartbeat traffic</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate delivery and promotion in JobService</name>
  <files>puppeteer/agent_service/services/job_service.py</files>
  <behavior>
    - In `receive_heartbeat`:
        - **Delivery**: If `node.pending_upgrade` exists, include it in the response dictionary.
        - **Promotion**: If `hb.upgrade_result` is present:
            - If status is `SUCCESS`:
                - Update `node.expected_capabilities` with the new tool.
                - Log transaction to `node.upgrade_history`.
                - Clear `node.pending_upgrade`.
                - Set `node.status = "ONLINE"`.
            - If status is `FAILED`:
                - Log failure to `node.upgrade_history`.
                - Clear `node.pending_upgrade`.
                - Set `node.status = "ONLINE"` (or keep restricted if preferred).
  </behavior>
  <action>
    Update `receive_heartbeat` logic in `puppeteer/agent_service/services/job_service.py`.
  </action>
  <verify>
    <automated>grep -q "hb.upgrade_result" puppeteer/agent_service/services/job_service.py && echo "Server C2 OK"</automated>
  </verify>
  <done>Orchestrator manages the lifecycle of runtime mutations</done>
</task>

</tasks>

<verification>
1. Stage an upgrade on the server.
2. Observe node pick it up via heartbeat.
3. Observe node report success.
4. Verify `expected_capabilities` on server is updated.
</verification>

<success_criteria>
- End-to-end hot-upgrade loop is functional.
- Zero-Trust model is maintained (Authorized promotion only).
</success_criteria>

<output>
After completion, create `.planning/phases/03-hot-upgrade-engine/03-03-SUMMARY.md`
</output>
