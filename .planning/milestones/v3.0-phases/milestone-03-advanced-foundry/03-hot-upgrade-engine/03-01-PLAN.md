---
phase: 03-hot-upgrade-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/agent_service/db.py
  - puppeteer/agent_service/main.py
  - puppeteer/migration_v21.sql
autonomous: true
requirements:
  - HUE-01 (Persistence)
  - HUE-02 (Staging API)

must_haves:
  truths:
    - "Node ORM has pending_upgrade and upgrade_history columns"
    - "POST /nodes/{id}/upgrade accepts a capability_id and signs the recipe"
  artifacts:
    - path: "puppeteer/agent_service/db.py"
      provides: "Hot-upgrade persistence"
      contains: "pending_upgrade"
---

<objective>
Prepare the orchestrator database and provide an API for staging signed runtime upgrades.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/ROADMAP.md
@/home/thomas/Development/master_of_puppets/.planning/phases/03-hot-upgrade-engine/03-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add upgrade fields to Node in db.py</name>
  <files>puppeteer/agent_service/db.py</files>
  <action>
    Modify `db.py`:
    - Add `pending_upgrade`: `Mapped[Optional[str]] = mapped_column(Text, nullable=True)`
    - Add `upgrade_history`: `Mapped[Optional[str]] = mapped_column(Text, nullable=True)`
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && /home/thomas/Development/master_of_puppets/.venv/bin/python -c "from agent_service.db import Node; assert hasattr(Node, 'pending_upgrade'); print('Models OK')"</automated>
  </verify>
  <done>Database models are updated for upgrade tracking</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Upgrade Staging API in main.py</name>
  <files>puppeteer/agent_service/main.py</files>
  <behavior>
    - Endpoint: `POST /api/nodes/{node_id}/upgrade`
    - Payload: `{ "capability_id": int }`
    - Logic:
        1. Fetch `CapabilityMatrix` entry by ID.
        2. Construct `upgrade_task` object.
        3. Sign the `injection_recipe` using `SignatureService`.
        4. Save signed task to `node.pending_upgrade`.
        5. Set `node.status = "UPGRADING"`.
  </behavior>
  <action>
    Add the `stage_node_upgrade` endpoint to `main.py`.
  </action>
  <verify>
    <automated>grep -q "async def stage_node_upgrade" puppeteer/agent_service/main.py && echo "API OK"</automated>
  </verify>
  <done>Admins can now queue signed upgrades for nodes</done>
</task>

<task type="auto">
  <name>Task 3: Create migration_v21.sql</name>
  <files>puppeteer/migration_v21.sql</files>
  <action>
    Create `puppeteer/migration_v21.sql` with idempotent SQL for the new columns.
  </action>
  <verify>
    <automated>grep -q "pending_upgrade" puppeteer/migration_v21.sql && echo "Migration OK"</automated>
  </verify>
  <done>Schema migration is ready</done>
</task>

</tasks>

<verification>
1. `python -c "from agent_service.db import Node; ..."` succeeds.
2. `migration_v21.sql` contains correct `ALTER` statements.
</verification>

<success_criteria>
- Nodes can hold a pending upgrade payload.
- The orchestrator can cryptographically sign recipes before they are sent to nodes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hot-upgrade-engine/03-01-SUMMARY.md`
</output>
