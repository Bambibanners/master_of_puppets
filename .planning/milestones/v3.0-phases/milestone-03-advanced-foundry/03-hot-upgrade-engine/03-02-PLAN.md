---
phase: 03-hot-upgrade-engine
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - puppets/environment_service/node.py
autonomous: true
requirements:
  - HUE-03 (Agent Execution)

must_haves:
  truths:
    - "Node can execute shell recipes and capture results"
    - "Node verifies recipe signatures against the master public key"
    - "Node can download artifacts from the orchestrator vault"
  artifacts:
    - path: "puppets/environment_service/node.py"
      provides: "Hot-upgrade execution engine"
      contains: "def execute_upgrade"
---

<objective>
Enable the Puppet Agent to securely execute injection recipes and verify the results.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/ROADMAP.md
@/home/thomas/Development/master_of_puppets/.planning/phases/03-hot-upgrade-engine/03-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement UpgradeManager in node.py</name>
  <files>puppets/environment_service/node.py</files>
  <behavior>
    - Add `UpgradeManager` class (or methods to `Node` class).
    - Method: `execute_upgrade(task: Dict)`:
        1. Verify signature of `recipe`.
        2. If `artifact_url` present, download to `/tmp`.
        3. Execute `recipe` as shell script (capture stdout/err).
        4. Execute `validation_cmd`.
        5. Return structured result.
  </behavior>
  <action>
    Modify `node.py` to add the upgrade execution logic.
  </action>
  <verify>
    <automated>grep -q "def execute_upgrade" puppets/environment_service/node.py && echo "Execution Engine OK"</automated>
  </verify>
  <done>Puppet agents can now perform in-place mutations</done>
</task>

</tasks>

<verification>
1. Mock an upgrade task with a valid signature.
2. Call `execute_upgrade` and verify the recipe runs.
3. Verify signature check rejects tempered recipes.
</verification>

<success_criteria>
- Nodes can pull binaries and execute arbitrary (signed) instructions.
- Success/Failure is captured reliably for reporting.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hot-upgrade-engine/03-02-SUMMARY.md`
</output>
