---
phase: 01-automation-apis
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - puppeteer/dashboard/src/views/Admin.tsx
autonomous: true
requirements:
  - AUT-03 (Trigger UI)

must_haves:
  truths:
    - "Automation tab exists in Admin view"
    - "Admins can create and delete triggers"
    - "UI provides a 'Copy Curl' helper for each trigger"
  artifacts:
    - path: "puppeteer/dashboard/src/views/Admin.tsx"
      provides: "Trigger management interface"
      contains: "<TabsTrigger value="automation">"
---

<objective>
Provide an administrative interface for creating and managing high-performance automation triggers.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/ROADMAP.md
@/home/thomas/Development/master_of_puppets/.planning/phases/01-automation-apis/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Automation tab to Admin view</name>
  <files>puppeteer/dashboard/src/views/Admin.tsx</files>
  <action>
    Update `Admin.tsx`:
    1. Add "Automation" to the `TabsList`.
    2. Create `TriggerManager` component to be rendered in `TabsContent`.
    3. `TriggerManager` should include:
        - A table of existing triggers (Name, Slug, Target Job).
        - A "Create Trigger" dialog.
        - Action buttons: "Copy Curl", "Delete".
  </action>
  <verify>
    <automated>grep -q "automation" puppeteer/dashboard/src/views/Admin.tsx && echo "UI Tab OK"</automated>
  </verify>
  <done>Automation management UI is live</done>
</task>

</tasks>

<verification>
1. Navigate to /admin -> Automation.
2. Create a new trigger.
3. Click "Copy Curl" and verify the generated command contains the correct URL and token.
</verification>

<success_criteria>
- Operators can easily provision machine-access tokens for specific jobs.
- Integration documentation (curl) is generated automatically.
</success_criteria>

<output>
After completion, create `.planning/phases/01-automation-apis/01-03-SUMMARY.md`
</output>
