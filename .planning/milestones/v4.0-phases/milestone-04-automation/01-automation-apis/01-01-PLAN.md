---
phase: 01-automation-apis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/agent_service/db.py
  - puppeteer/agent_service/models.py
  - puppeteer/migration_v23.sql
autonomous: true
requirements:
  - AUT-01 (Data Model)

must_haves:
  truths:
    - "Trigger ORM class exists with slug, secret_token, and job_definition_id"
    - "TriggerResponse and TriggerCreate models are defined"
    - "migration_v23.sql contains idempotent CREATE TABLE for triggers"
  artifacts:
    - path: "puppeteer/agent_service/db.py"
      provides: "Automation metadata storage"
      contains: "class Trigger"
    - path: "puppeteer/migration_v23.sql"
      provides: "Schema update for automation"
      contains: "CREATE TABLE IF NOT EXISTS triggers"
---

<objective>
Establish the database schema and data models for the new Trigger system.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/ROADMAP.md
@/home/thomas/Development/master_of_puppets/.planning/phases/01-automation-apis/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Trigger model to db.py</name>
  <files>puppeteer/agent_service/db.py</files>
  <action>
    Modify `db.py`:
    Add `Trigger` table: `id` (UUID), `slug` (String, Unique), `name` (String), `job_definition_id` (String, FK to scheduled_jobs), `secret_token` (String), `is_active` (Boolean), `created_at`.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && /home/thomas/Development/master_of_puppets/.venv/bin/python -c "from agent_service.db import Trigger; print('Model OK')"</automated>
  </verify>
  <done>Trigger database model is defined</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define Pydantic models in models.py</name>
  <files>puppeteer/agent_service/models.py</files>
  <action>
    Update `models.py`:
    1. Add `TriggerCreate(BaseModel)`.
    2. Add `TriggerResponse(BaseModel)`.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && /home/thomas/Development/master_of_puppets/.venv/bin/python -c "from agent_service.models import TriggerResponse; print('Models OK')"</automated>
  </verify>
  <done>Data contracts for triggers are defined</done>
</task>

<task type="auto">
  <name>Task 3: Create migration_v23.sql</name>
  <files>puppeteer/migration_v23.sql</files>
  <action>
    Create `puppeteer/migration_v23.sql` with idempotent SQL for the `triggers` table.
  </action>
  <verify>
    <automated>grep -q "triggers" puppeteer/migration_v23.sql && echo "Migration OK"</automated>
  </verify>
  <done>Postgres migration file is ready</done>
</task>

</tasks>

<verification>
1. `python -c "from agent_service.db import Trigger; ..."` succeeds.
2. `migration_v23.sql` contains `CREATE TABLE IF NOT EXISTS triggers`.
</verification>

<success_criteria>
- System is ready to persist automation trigger definitions.
- Triggers are correctly linked to scheduled job definitions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-automation-apis/01-01-SUMMARY.md`
</output>
