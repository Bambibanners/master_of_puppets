---
phase: 01-automation-apis
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - puppeteer/agent_service/main.py
  - puppeteer/agent_service/services/trigger_service.py
autonomous: true
requirements:
  - AUT-02 (Trigger API)

must_haves:
  truths:
    - "Public /api/trigger/{slug} endpoint exists"
    - "Trigger requests are authenticated via X-MOP-Trigger-Key header"
    - "Triggering a slug correctly launches the associated ScheduledJob"
  artifacts:
    - path: "puppeteer/agent_service/services/trigger_service.py"
      provides: "Trigger resolution logic"
      contains: "async def fire_trigger"
---

<objective>
Implement the core automation logic and the public-facing trigger API.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/ROADMAP.md
@/home/thomas/Development/master_of_puppets/.planning/phases/01-automation-apis/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TriggerService</name>
  <files>puppeteer/agent_service/services/trigger_service.py</files>
  <action>
    Create `puppeteer/agent_service/services/trigger_service.py`.
    1. Implement `fire_trigger(slug: str, token: str, payload_data: Dict, db: AsyncSession)`:
        - Fetch active Trigger by slug.
        - Verify token matches.
        - Fetch the associated `ScheduledJob`.
        - Create an ad-hoc `Job` based on the scheduled job's script, tags, etc.
        - Merge `payload_data` into the job's context.
        - Call `JobService.create_job`.
  </action>
  <verify>
    <automated>ls puppeteer/agent_service/services/trigger_service.py && echo "Service OK"</automated>
  </verify>
  <done>Trigger service is ready to handle automation requests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Trigger endpoints in main.py</name>
  <files>puppeteer/agent_service/main.py</files>
  <action>
    Add routes to `main.py`:
    1. `POST /api/trigger/{slug}`: Public endpoint. Does NOT require `get_current_user`. Uses `X-MOP-Trigger-Key` header.
    2. `GET/POST/DELETE /api/admin/triggers`: Management endpoints for admins.
  </action>
  <verify>
    <automated>grep -q "app.post("/api/trigger/{slug}")" puppeteer/agent_service/main.py && echo "API OK"</automated>
  </verify>
  <done>Automation APIs are live</done>
</task>

</tasks>

<verification>
1. `curl -X POST .../api/trigger/my-slug -H "X-MOP-Trigger-Key: my-token"` results in a new job being created.
2. Invalid token returns 401.
3. Inactive trigger returns 403.
</verification>

<success_criteria>
- CI/CD pipelines can trigger jobs using a single token.
- Triggers are isolated from the main user authentication flow.
- Audit logs capture trigger executions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-automation-apis/01-02-SUMMARY.md`
</output>
