---
phase: 02-conditional-triggers
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - puppeteer/dashboard/src/views/Jobs.tsx
autonomous: true
requirements:
  - CON-04 (UI Visualization)

must_haves:
  truths:
    - "Jobs list displays 'Waiting for Signal' badge when appropriate"
    - "Job detail panel shows specific unblocking conditions"
  artifacts:
    - path: "puppeteer/dashboard/src/views/Jobs.tsx"
      provides: "Reactive dependency UI"
      contains: "r.type === 'signal'"
---

<objective>
Update the dashboard to accurately represent complex dependencies and signal-based blocking.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/ROADMAP.md
@/home/thomas/Development/master_of_puppets/.planning/phases/02-conditional-triggers/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Dependency display in JobDetailPanel</name>
  <files>puppeteer/dashboard/src/views/Jobs.tsx</files>
  <action>
    Modify `JobDetailPanel`:
    - Handle both string and object dependencies.
    - For objects of type 'job': Display condition (e.g., "Wait for [GUID] to FAIL").
    - For objects of type 'signal': Display "Wait for Signal: [NAME]" with a distinct icon.
  </action>
  <verify>
    <automated>grep -q "Wait for Signal" puppeteer/dashboard/src/views/Jobs.tsx && echo "UI Logic OK"</automated>
  </verify>
  <done>UI clearly surfaces why a job is blocked and what it's waiting for</done>
</task>

</tasks>

<verification>
1. Select a job waiting for a signal. Verify the "Depends On" section shows the signal name correctly.
2. Select a job with a FAILED condition. Verify the UI reflects this requirement.
</verification>

<success_criteria>
- Operators have visibility into complex workflow states.
- Conditional dependencies are easy to debug from the dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/02-conditional-triggers/02-04-SUMMARY.md`
</output>
