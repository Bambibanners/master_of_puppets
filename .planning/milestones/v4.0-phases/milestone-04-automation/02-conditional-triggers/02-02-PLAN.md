---
phase: 02-conditional-triggers
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - puppeteer/agent_service/main.py
autonomous: true
requirements:
  - CON-02 (Signal API)

must_haves:
  truths:
    - "POST /api/signals/{name} exists and creates Signal records"
    - "Signal API is protected by service principal authentication"
  artifacts:
    - path: "puppeteer/agent_service/main.py"
      provides: "Signal ingest endpoint"
      contains: "app.post("/api/signals/{name}")"
---

<objective>
Expose a secure API for external systems to fire signals and unblock dependent jobs.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/ROADMAP.md
@/home/thomas/Development/master_of_puppets/.planning/phases/02-conditional-triggers/02-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Signal API endpoints in main.py</name>
  <files>puppeteer/agent_service/main.py</files>
  <action>
    Add routes to `main.py`:
    1. `POST /api/signals/{name}`: Public-facing (for SPs). Restricted to `operator` role or SP with appropriate permission.
    2. `GET /api/signals`: List all fired signals (Admin Only).
    3. `DELETE /api/signals/{name}`: Clear a signal (Admin Only).
  </action>
  <verify>
    <automated>grep -q "app.post("/api/signals/{name}")" puppeteer/agent_service/main.py && echo "API OK"</automated>
  </verify>
  <done>Signal APIs are implemented and secured</done>
</task>

</tasks>

<verification>
1. `curl -X POST .../api/signals/my-signal` returns 200.
2. Signal exists in `GET /api/signals` list.
</verification>

<success_criteria>
- Signals can be fired externally via API.
- Security is maintained through existing Service Principal / API Key logic.
</success_criteria>

<output>
After completion, create `.planning/phases/02-conditional-triggers/02-02-SUMMARY.md`
</output>
