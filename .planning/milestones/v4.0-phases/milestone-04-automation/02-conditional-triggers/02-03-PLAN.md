---
phase: 02-conditional-triggers
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - puppeteer/agent_service/services/job_service.py
autonomous: true
requirements:
  - CON-03 (Orchestration Engine)

must_haves:
  truths:
    - "Unblocking logic supports COMPLETED, FAILED, and ANY conditions"
    - "Unblocking logic supports named Signals"
    - "Backward compatibility for plain GUID strings is maintained"
  artifacts:
    - path: "puppeteer/agent_service/services/job_service.py"
      provides: "Reactive dependency engine"
      contains: "def _check_dependency_met"
---

<objective>
Harden and extend the dependency engine to support complex conditions and reactive unblocking.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/ROADMAP.md
@/home/thomas/Development/master_of_puppets/.planning/phases/02-conditional-triggers/02-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement dependency check helper</name>
  <files>puppeteer/agent_service/services/job_service.py</files>
  <behavior>
    - Create `_check_dependency_met(dep: Union[str, Dict], db: AsyncSession)`:
        - If string: return True if Job(GUID) is `COMPLETED`.
        - If dict (type='job'): check `condition` (COMPLETED, FAILED, ANY).
        - If dict (type='signal'): return True if Signal(ref) exists in `signals` table.
  </behavior>
  <action>
    Add helper logic to `JobService`.
  </action>
  <verify>
    <automated>grep -q "def _check_dependency_met" puppeteer/agent_service/services/job_service.py && echo "Helper OK"</automated>
  </verify>
  <done>Granular dependency checking is available</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor _unblock_dependents</name>
  <files>puppeteer/agent_service/services/job_service.py</files>
  <behavior>
    - Update `_unblock_dependents` to use the new helper.
    - Ensure it is triggered correctly from `report_result` for all terminal states.
  </behavior>
  <action>
    Update `_unblock_dependents` in `puppeteer/agent_service/services/job_service.py`.
  </action>
  <verify>
    <automated>grep -q "all(await JobService._check_dependency_met" puppeteer/agent_service/services/job_service.py && echo "Engine OK"</automated>
  </verify>
  <done>Sequential dependency engine is now a reactive conditional engine</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Signal unblocking trigger</name>
  <files>puppeteer/agent_service/services/job_service.py</files>
  <action>
    Implement `unblock_jobs_by_signal(signal_name: str, db: AsyncSession)`:
    - Find all `BLOCKED` jobs that depend on this signal name.
    - Check if all their other dependencies are met.
    - Transition to `PENDING` if yes.
  </action>
  <verify>
    <automated>grep -q "def unblock_jobs_by_signal" puppeteer/agent_service/services/job_service.py && echo "Signal Trigger OK"</automated>
  </verify>
  <done>External signals can now release blocked jobs</done>
</task>

</tasks>

<verification>
1. Job B depends on Job A (condition: FAILED). Terminally fail Job A. Verify Job B becomes PENDING.
2. Job C depends on Signal 'GO'. Fire signal 'GO'. Verify Job C becomes PENDING.
</verification>

<success_criteria>
- Multi-step workflows can handle failure branches (e.g., Run Job B only if Job A fails).
- Workflows can pause for external approval (Signals).
</success_criteria>

<output>
After completion, create `.planning/phases/02-conditional-triggers/02-03-SUMMARY.md`
</output>
