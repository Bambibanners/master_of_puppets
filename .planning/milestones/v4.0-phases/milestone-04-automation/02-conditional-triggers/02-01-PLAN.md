---
phase: 02-conditional-triggers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/agent_service/db.py
  - puppeteer/agent_service/models.py
  - puppeteer/migration_v24.sql
autonomous: true
requirements:
  - CON-01 (Signal Model)

must_haves:
  truths:
    - "Signal ORM class exists with name and payload"
    - "migration_v24.sql contains idempotent CREATE TABLE for signals"
  artifacts:
    - path: "puppeteer/agent_service/db.py"
      provides: "Signal persistence"
      contains: "class Signal"
---

<objective>
Prepare the database for reactive signal tracking.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/ROADMAP.md
@/home/thomas/Development/master_of_puppets/.planning/phases/02-conditional-triggers/02-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Signal model to db.py</name>
  <files>puppeteer/agent_service/db.py</files>
  <action>
    Modify `db.py`:
    Add `Signal` table: `name` (String, Primary Key), `payload` (Text JSON), `created_at`.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && /home/thomas/Development/master_of_puppets/.venv/bin/python -c "from agent_service.db import Signal; print('Model OK')"</automated>
  </verify>
  <done>Signal database model is defined</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define Signal Pydantic models</name>
  <files>puppeteer/agent_service/models.py</files>
  <action>
    Update `models.py`:
    Add `SignalFire(BaseModel)` and `SignalResponse(BaseModel)`.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && /home/thomas/Development/master_of_puppets/.venv/bin/python -c "from agent_service.models import SignalFire; print('Models OK')"</automated>
  </verify>
  <done>Data contracts for signals are defined</done>
</task>

<task type="auto">
  <name>Task 3: Create migration_v24.sql</name>
  <files>puppeteer/migration_v24.sql</files>
  <action>
    Create `puppeteer/migration_v24.sql` with idempotent SQL for the `signals` table.
  </action>
  <verify>
    <automated>grep -q "signals" puppeteer/migration_v24.sql && echo "Migration OK"</automated>
  </verify>
  <done>Postgres migration file is ready</done>
</task>

</tasks>

<verification>
1. `python -c "from agent_service.db import Signal; ..."` succeeds.
2. `migration_v24.sql` contains `CREATE TABLE IF NOT EXISTS signals`.
</verification>

<success_criteria>
- The system can persist external signals.
- Models are correctly typed for FastAPI integration.
</success_criteria>

<output>
After completion, create `.planning/phases/02-conditional-triggers/02-01-SUMMARY.md`
</output>
