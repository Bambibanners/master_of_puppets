---
phase: 03-headless-sdk
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - mop_sdk/client.py
autonomous: true
requirements:
  - SDK-02 (Signing & Orchestration)

must_haves:
  truths:
    - "SDK can sign python_script jobs automatically using a PEM key"
    - "wait_for_job method polls with backoff"
  artifacts:
    - path: "mop_sdk/client.py"
      provides: "Secure job submission"
      contains: "def submit_python_job"
---

<objective>
Implement secure job submission with automatic signing and a convenience method for job polling.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/ROADMAP.md
@/home/thomas/Development/master_of_puppets/.planning/phases/03-headless-sdk/03-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement submit_python_job with automatic signing</name>
  <files>mop_sdk/client.py</files>
  <action>
    Add `submit_python_job(script, private_key_path, tags=None, ...)` method:
    1. Read the Ed25519 private key from path.
    2. Generate signature for the script.
    3. Construct the `JobCreate` payload.
    4. Call `POST /jobs`.
  </action>
  <verify>
    <automated>grep -q "def submit_python_job" mop_sdk/client.py && echo "Submission logic OK"</automated>
  </verify>
  <done>SDK handles script signing internally</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement wait_for_job polling</name>
  <files>mop_sdk/client.py</files>
  <action>
    Add `wait_for_job(guid, timeout=300, interval=2)` method:
    - Polls `GET /jobs` (or specific status endpoint).
    - Returns when status is terminal (`COMPLETED`, `FAILED`, `CANCELLED`).
    - Raises timeout error if exceeded.
  </action>
  <verify>
    <automated>grep -q "def wait_for_job" mop_sdk/client.py && echo "Polling logic OK"</automated>
  </verify>
  <done>SDK provides convenience methods for synchronous-style wait</done>
</task>

</tasks>

<verification>
1. Code review of `submit_python_job` confirms `cryptography` usage for signing.
2. Code review of `wait_for_job` confirms loop and terminal state checks.
</verification>

<success_criteria>
- Developers can submit secure jobs with a single function call.
- The SDK abstracts the complexities of the pull-architecture's status tracking.
</success_criteria>

<output>
After completion, create `.planning/phases/03-headless-sdk/03-02-SUMMARY.md`
</output>
