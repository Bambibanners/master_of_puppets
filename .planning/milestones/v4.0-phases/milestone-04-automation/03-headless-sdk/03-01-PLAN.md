---
phase: 03-headless-sdk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mop_sdk/client.py
  - mop_sdk/__init__.py
autonomous: true
requirements:
  - SDK-01 (Foundation)

must_haves:
  truths:
    - "MOPClient supports JWT login and API Key authentication"
    - "Client can list jobs and retrieve specific job status"
  artifacts:
    - path: "mop_sdk/client.py"
      provides: "Base SDK client"
      contains: "class MOPClient"
---

<objective>
Implement the foundational Python SDK client with support for multiple authentication methods and basic job management.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/ROADMAP.md
@/home/thomas/Development/master_of_puppets/.planning/phases/03-headless-sdk/03-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold mop_sdk package</name>
  <files>
    - mop_sdk/__init__.py
    - mop_sdk/client.py
  </files>
  <action>
    Create the `mop_sdk` directory and initial files.
    Implement `MOPClient` class in `client.py` with:
    - `__init__(base_url, api_key=None, username=None, password=None)`
    - Internal token management logic.
  </action>
  <verify>
    <automated>python3 -c "from mop_sdk import MOPClient; print('Import OK')"</automated>
  </verify>
  <done>SDK package is structured and importable</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Authentication logic</name>
  <files>mop_sdk/client.py</files>
  <action>
    Implement:
    - `_authenticate()`: Handles JWT login if username/password provided.
    - Header management: Uses `X-API-Key` for service principals or `Authorization: Bearer` for JWT.
  </action>
  <verify>
    <automated>grep -q "Authorization" mop_sdk/client.py && echo "Auth logic OK"</automated>
  </verify>
  <done>Client correctly handles system authentication</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Job and Node listing</name>
  <files>mop_sdk/client.py</files>
  <action>
    Add methods:
    - `list_jobs(skip=0, limit=50)`
    - `get_job(guid)`
    - `list_nodes()`
    - `fire_signal(name, payload=None)`
  </action>
  <verify>
    <automated>grep -q "def list_jobs" mop_sdk/client.py && echo "Methods OK"</automated>
  </verify>
  <done>Basic resource management is available via SDK</done>
</task>

</tasks>

<verification>
1. `from mop_sdk import MOPClient` succeeds.
2. `MOPClient(base_url="http://localhost:8000")` initializes without error.
</verification>

<success_criteria>
- Python developers can instantiate a client and authenticate against the platform.
- Basic "Read" operations are functional.
</success_criteria>

<output>
After completion, create `.planning/phases/03-headless-sdk/03-01-SUMMARY.md`
</output>
