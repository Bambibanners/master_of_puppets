---
phase: 03-lifecycle-apis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/dashboard/src/views/Templates.tsx
  - puppeteer/dashboard/src/views/Admin.tsx
  - puppeteer/dashboard/src/views/Signatures.tsx
autonomous: true
requirements:
  - GAP-1 (frontend)

must_haves:
  truths:
    - "Delete buttons exist for Blueprints, Service Principals, and Signatures"
    - "Conflict errors (409) are correctly displayed to the user"
  artifacts:
    - path: "puppeteer/dashboard/src/views/Templates.tsx"
      provides: "Delete button for Blueprints"
      contains: "const deleteMutation = useMutation"
---

<objective>
Complete the administrative lifecycle by adding delete functionality to the remaining system entities in the dashboard.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/PROJECT.md
@/home/thomas/Development/master_of_puppets/.planning/phases/03-lifecycle-apis/03-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Delete functionality to BlueprintItem</name>
  <files>puppeteer/dashboard/src/views/Templates.tsx</files>
  <action>
    Update `BlueprintItem` in `puppeteer/dashboard/src/views/Templates.tsx`:
    1. Implement `deleteMutation` for blueprints.
    2. Add a `Trash2` button to the `BlueprintItem` card.
    3. Ensure 409 Conflict errors (references by templates) are caught and displayed via toast.
  </action>
  <verify>
    <automated>grep -q "deleteMutation" puppeteer/dashboard/src/views/Templates.tsx && echo "Blueprint Delete OK"</automated>
  </verify>
  <done>Blueprints can be deleted from the UI</done>
</task>

<task type="auto">
  <name>Task 2: Verify and Add Delete to Service Principals</name>
  <files>puppeteer/dashboard/src/views/Admin.tsx</files>
  <action>
    Check `Admin.tsx`. If delete buttons are missing from the Service Principals table, add them.
  </action>
  <verify>
    <automated>ls puppeteer/dashboard/src/views/Admin.tsx && echo "File verified"</automated>
  </verify>
  <done>Service Principals can be managed/deleted</done>
</task>

<task type="auto">
  <name>Task 3: Verify and Add Delete to Signatures</name>
  <files>puppeteer/dashboard/src/views/Signatures.tsx</files>
  <action>
    Check `Signatures.tsx`. If delete buttons are missing from the Signatures list, add them.
  </action>
  <verify>
    <automated>ls puppeteer/dashboard/src/views/Signatures.tsx && echo "File verified"</automated>
  </verify>
  <done>Signatures can be managed/deleted</done>
</task>

</tasks>

<verification>
1. Click Delete on a Blueprint. Confirm toast success.
2. Click Delete on a Blueprint referenced by a Template. Confirm Conflict toast appears.
3. Confirm Delete buttons are present in Admin and Signatures views.
</verification>

<success_criteria>
- No "dangling" entities that cannot be removed via UI.
- Consistent user feedback for lifecycle actions.
</success_criteria>

<output>
After completion, create `.planning/phases/03-lifecycle-apis/03-01-SUMMARY.md`
</output>
