---
phase: 04-provisioning-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/dashboard/src/components/ManageMountsModal.tsx
  - puppeteer/dashboard/src/views/Templates.tsx
autonomous: true
requirements:
  - GAP-6
  - BUG-6 (feedback)

must_haves:
  truths:
    - "ManageMountsModal allows adding/removing network mounts"
    - "Templates view can display build failure details"
  artifacts:
    - path: "puppeteer/dashboard/src/components/ManageMountsModal.tsx"
      provides: "Network mount management UI"
      contains: "export default ManageMountsModal"
---

<objective>
Complete the dashboard UI for network mount management and provide detailed feedback for image build operations.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/PROJECT.md
@/home/thomas/Development/master_of_puppets/.planning/phases/04-provisioning-ui/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ManageMountsModal component</name>
  <files>puppeteer/dashboard/src/components/ManageMountsModal.tsx</files>
  <action>
    Create `puppeteer/dashboard/src/components/ManageMountsModal.tsx`. 
    - Implement a dialog that fetches existing mounts from `GET /config/mounts`.
    - Provide a list of editable rows (Name, Path).
    - Provide "Add Mount" and "Delete" buttons for rows.
    - Submit the list to `POST /config/mounts` on save.
  </action>
  <verify>
    <automated>ls puppeteer/dashboard/src/components/ManageMountsModal.tsx && echo "File exists"</automated>
  </verify>
  <done>ManageMountsModal is implemented and ready for use</done>
</task>

<task type="auto">
  <name>Task 2: Enhance Templates.tsx with Build Details</name>
  <files>puppeteer/dashboard/src/views/Templates.tsx</files>
  <action>
    Update `Templates.tsx`:
    - When a build fails, show a "Details" button or tooltip.
    - Implement a simple modal or expansion that displays the full `template.status` string (which contains build error logs).
  </action>
  <verify>
    <automated>grep -q "Details" puppeteer/dashboard/src/views/Templates.tsx && echo "Build Feedback OK"</automated>
  </verify>
  <done>Templates view provides actionable feedback on build failures</done>
</task>

</tasks>

<verification>
1. Open "Network Mounts" from the Nodes page. Add a mount and save. Verify the API call success.
2. Trigger a build that is guaranteed to fail (e.g., invalid blueprint). Click "Details" and verify error output visibility.
</verification>

<success_criteria>
- Operators can configure global network mounts without manual DB edits.
- Image build failures are easy to diagnose from the UI.
</success_criteria>

<output>
After completion, create `.planning/phases/04-provisioning-ui/04-01-SUMMARY.md`
</output>
