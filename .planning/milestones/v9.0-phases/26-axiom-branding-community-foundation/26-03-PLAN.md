---
phase: 26-axiom-branding-community-foundation
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/mkdocs.yml
  - docs/docs/developer/architecture.md
  - docs/docs/feature-guides/axiom-push.md
  - docs/docs/feature-guides/job-scheduling.md
  - docs/docs/feature-guides/oauth.md
  - docs/docs/runbooks/jobs.md
  - docs/docs/index.md
  - docs/docs/getting-started/prerequisites.md
  - docs/docs/getting-started/first-job.md
  - docs/docs/feature-guides/foundry.md
  - docs/docs/feature-guides/rbac.md
  - docs/docs/feature-guides/rbac-reference.md
  - docs/docs/security/index.md
  - docs/docs/security/mtls.md
  - docs/docs/security/air-gap.md
  - docs/docs/security/audit-log.md
  - docs/docs/security/rbac-hardening.md
  - docs/docs/runbooks/faq.md
  - docs/docs/runbooks/foundry.md
  - docs/docs/developer/contributing.md
  - docs/docs/developer/setup-deployment.md
  - docs/docs/api-reference/index.md
autonomous: true
requirements:
  - BRAND-06

must_haves:
  truths:
    - "grep -r 'mop-push' docs/docs/ returns zero results"
    - "grep -r 'Master of Puppets' docs/docs/ returns zero results"
    - "grep 'site_name: Axiom' docs/mkdocs.yml returns a match"
    - "The renamed file docs/docs/feature-guides/axiom-push.md exists and the old mop-push.md does not"
    - "Docker docs build (mkdocs build --strict) passes with no nav reference errors"
    - "Mermaid diagrams in architecture.md have been updated in prose labels but internal code identifiers in diagram node IDs are preserved"
  artifacts:
    - path: "docs/mkdocs.yml"
      provides: "Updated site_name and nav entry pointing to axiom-push.md"
      contains: "site_name: Axiom"
    - path: "docs/docs/feature-guides/axiom-push.md"
      provides: "Renamed CLI guide file"
    - path: "docs/docs/developer/architecture.md"
      provides: "Architecture guide with Axiom naming throughout prose and diagram labels"
  key_links:
    - from: "docs/mkdocs.yml nav"
      to: "docs/docs/feature-guides/axiom-push.md"
      via: "axiom-push CLI nav entry"
      pattern: "axiom-push CLI: feature-guides/axiom-push"
---

<objective>
Complete the MkDocs docs site naming pass — rename `mop-push.md` to `axiom-push.md`, update `mkdocs.yml` (site_name + nav), and replace all occurrences of old branding terms (Puppeteer, Puppet Node/node, MoP, Master of Puppets, mop-push) with Axiom equivalents across all 21 docs files.

Purpose: The docs site is the primary face of Axiom for operators and contributors. Old MoP/Puppeteer/Puppet branding across the entire docs corpus creates a confusing identity mismatch. This pass makes every page consistently Axiom-branded.

Output: All 21 docs/*.md files updated with Axiom naming, mkdocs.yml updated, mop-push.md renamed to axiom-push.md.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-axiom-branding-community-foundation/26-CONTEXT.md

<interfaces>
<!-- Naming substitution map (from research — LOCKED DECISIONS) -->

| Find | Replace with | Notes |
|------|-------------|-------|
| `Master of Puppets` | `Axiom` | Brand name |
| `\bMoP\b` | `Axiom` | Acronym — word-boundary match to avoid "MoP" in other contexts |
| `Puppeteer` (as component in prose) | `Axiom Orchestrator` (first use per file) / `orchestrator` (subsequent) | Component name — ONLY in prose, NOT in code blocks that reflect internal API routes/class names |
| `Puppet Node` | `Axiom Node` (first use per file) / `node` (subsequent) | Component name |
| `Puppet node` | `Axiom Node` (first use per file) / `node` (subsequent) | Component name |
| `puppet node` | `Axiom Node` (first use per file) / `node` (subsequent) | lowercase variant |
| `mop-push` | `axiom-push` | CLI name |
| `mop_push` | `axiom_push` | CLI underscore variant (if present) |
| `mop-push.md` | `axiom-push.md` | Filename reference |
| `feature-guides/mop-push` | `feature-guides/axiom-push` | Nav/link reference |

CRITICAL — DO NOT REPLACE:
- Code identifiers in Python source code (no .py files touched in this plan)
- Internal API route paths in code blocks: `/api/enroll`, `/work/pull`, `/heartbeat`
- Internal code identifiers in Mermaid diagram node IDs (e.g., `participant P as Puppeteer` → update the display name but preserve node ID if it's an identifier). For Mermaid: update the LABEL (after `as`) but be careful about whether the node ID itself appears in prose.
- The word "puppet" as a plain English noun (not referring to the Puppet Node component)
- DB table names, class names, function names appearing in code blocks

Pattern for "introduce on first use, short form after" within each file:
- First occurrence of the Puppeteer component → "Axiom Orchestrator (orchestrator)" or just "Axiom Orchestrator"
- Subsequent → "orchestrator"
- First occurrence of Puppet Node → "Axiom Node (node)" or just "Axiom Node"
- Subsequent → "node"

Note: In many files the component has already appeared many times. For files with 1-3 hits, replace all with "Axiom Orchestrator" / "Axiom Node". For files with many hits, use the first-use/short-form pattern.

File rename requirement:
- `docs/docs/feature-guides/mop-push.md` → `docs/docs/feature-guides/axiom-push.md`
- This MUST happen in the same task as the mkdocs.yml nav update or strict mode will break

mkdocs.yml changes:
- Line 1: `site_name: Master of Puppets` → `site_name: Axiom`
- Nav entry: `- mop-push CLI: feature-guides/mop-push.md` → `- axiom-push CLI: feature-guides/axiom-push.md`

architecture.md special care:
- This file has 26 hits — heaviest file
- Contains Mermaid diagrams with `subgraph Puppeteer [...]` and `participant P as Puppeteer`
  - Update subgraph labels and participant display names to Axiom Orchestrator
  - Keep Mermaid node IDs short (e.g., `P`) unchanged — they don't appear in rendered output
- Prose section headers reference "Puppeteer" as an architectural component — update these
- Code blocks showing internal Python service names (e.g., `agent_service/main.py`) — leave filename unchanged
- Table rows describing components — update "Puppeteer" and "Puppet Node" column values to Axiom equivalents
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename mop-push.md, update mkdocs.yml, and update high-density docs files</name>
  <files>
    docs/mkdocs.yml,
    docs/docs/feature-guides/axiom-push.md,
    docs/docs/developer/architecture.md,
    docs/docs/feature-guides/job-scheduling.md,
    docs/docs/feature-guides/oauth.md,
    docs/docs/runbooks/jobs.md
  </files>
  <action>
Perform the critical file rename and high-density naming pass. These are the files with the most old-branding hits (26, 14, 10, 8, 6 respectively).

**Step 1 — Rename the file and update mkdocs.yml simultaneously:**
- Rename `docs/docs/feature-guides/mop-push.md` to `docs/docs/feature-guides/axiom-push.md` (git mv or create+delete)
- In `docs/mkdocs.yml` line 1: `site_name: Master of Puppets` → `site_name: Axiom`
- In `docs/mkdocs.yml` nav: `- mop-push CLI: feature-guides/mop-push.md` → `- axiom-push CLI: feature-guides/axiom-push.md`
- Inside the renamed `axiom-push.md`: replace all `mop-push` → `axiom-push` and `Master of Puppets` / `MoP` → `Axiom` in prose

**Step 2 — Update `docs/docs/developer/architecture.md` (26 hits, most complex):**
- Mermaid subgraph labels: `subgraph Puppeteer ["Puppeteer (Control Plane)"]` → update the label to `subgraph Puppeteer ["Axiom Orchestrator (Control Plane)"]` (the node ID `Puppeteer` in the subgraph declaration is fine to leave as the internal identifier — it does not render)
- Mermaid participant labels: `participant P as Puppeteer (agent_service)` → `participant P as Axiom Orchestrator (agent_service)`
- Mermaid participant labels: `participant N as Puppet Node` → `participant N as Axiom Node`
- Prose paragraphs: replace all `Puppeteer` (component references) with `Axiom Orchestrator` (first occurrence per paragraph cluster) / `orchestrator` (subsequent)
- Prose paragraphs: replace `Puppet Node` / `Puppet node` / `puppet node` with `Axiom Node` / `node`
- Table cells: `| **Puppeteer** |` → `| **Axiom Orchestrator** |`, `| **Puppet Node** |` → `| **Axiom Node** |`
- Page title and intro: `Master of Puppets is a distributed job orchestration platform...` → `Axiom is a distributed job orchestration platform...`
- Do NOT change: internal Python file paths (`puppeteer/agent_service/main.py`), class names in code examples, API route paths

**Step 3 — Update `docs/docs/feature-guides/job-scheduling.md` (6 hits):**
- Replace all `Puppeteer` component references → `orchestrator` (these are likely in context where "orchestrator" is immediately clear)
- Replace `MoP` / `Master of Puppets` → `Axiom`

**Step 4 — Update `docs/docs/feature-guides/oauth.md` (8 hits):**
- Replace `Puppeteer` component references → `Axiom Orchestrator` / `orchestrator`
- Replace `MoP` → `Axiom`
- Keep OAuth route paths unchanged

**Step 5 — Update `docs/docs/runbooks/jobs.md` (10 hits):**
- Replace `Puppeteer` → `orchestrator` (runbooks are operational context where short form is clear)
- Replace `Puppet node` / `Puppet Node` → `node` / `Axiom Node`
- Replace `Master of Puppets` / `MoP` → `Axiom`
  </action>
  <verify>
    <automated>test -f /home/thomas/Development/master_of_puppets/docs/docs/feature-guides/axiom-push.md && ! test -f /home/thomas/Development/master_of_puppets/docs/docs/feature-guides/mop-push.md && grep -q "site_name: Axiom" /home/thomas/Development/master_of_puppets/docs/mkdocs.yml && grep -q "axiom-push CLI: feature-guides/axiom-push.md" /home/thomas/Development/master_of_puppets/docs/mkdocs.yml && ! grep -q "mop-push" /home/thomas/Development/master_of_puppets/docs/mkdocs.yml && echo "PASS"</automated>
  </verify>
  <done>
    - `docs/docs/feature-guides/axiom-push.md` exists, `mop-push.md` does not
    - mkdocs.yml has `site_name: Axiom` and nav entry references `axiom-push.md`
    - architecture.md has no remaining `Master of Puppets` or `MoP` in prose
    - job-scheduling.md, oauth.md, runbooks/jobs.md have no old branding terms
  </done>
</task>

<task type="auto">
  <name>Task 2: Naming pass across remaining 16 docs files</name>
  <files>
    docs/docs/index.md,
    docs/docs/getting-started/prerequisites.md,
    docs/docs/getting-started/first-job.md,
    docs/docs/feature-guides/foundry.md,
    docs/docs/feature-guides/rbac.md,
    docs/docs/feature-guides/rbac-reference.md,
    docs/docs/security/index.md,
    docs/docs/security/mtls.md,
    docs/docs/security/air-gap.md,
    docs/docs/security/audit-log.md,
    docs/docs/security/rbac-hardening.md,
    docs/docs/runbooks/faq.md,
    docs/docs/runbooks/foundry.md,
    docs/docs/developer/contributing.md,
    docs/docs/developer/setup-deployment.md,
    docs/docs/api-reference/index.md
  </files>
  <action>
Apply the naming substitution map to the remaining 16 docs files. Each has low-to-medium hit counts (1-5 each). These are mechanical substitutions following the same rules as Task 1.

For each file, apply:
- `Master of Puppets` → `Axiom`
- `\bMoP\b` → `Axiom` (word-boundary to avoid false matches)
- `Puppeteer` (component in prose) → `Axiom Orchestrator` or `orchestrator` depending on context
- `Puppet Node` / `Puppet node` / `puppet node` → `Axiom Node` or `node` depending on context
- `mop-push` → `axiom-push`

File-specific notes:
- `docs/docs/index.md` (5 hits) — landing page; use full form "Axiom Orchestrator" on first mention, then "orchestrator". Replace product name in title/intro.
- `docs/docs/getting-started/prerequisites.md` (2 hits) — introductory context; use "Axiom Orchestrator" / "Axiom Node" in full on first use (reader is new).
- `docs/docs/getting-started/first-job.md` (5 hits) — use "Axiom Node" / "orchestrator" per context.
- `docs/docs/feature-guides/foundry.md` (1 hit) — single substitution.
- `docs/docs/feature-guides/rbac.md` (1 hit) — single substitution.
- `docs/docs/feature-guides/rbac-reference.md` (1 hit) — single substitution.
- `docs/docs/security/index.md` (2 hits) — security landing page; replace product name.
- `docs/docs/security/mtls.md` (3 hits) — mTLS guide references "Puppeteer" as the CA operator. Replace with "orchestrator" where it refers to the control plane component.
- `docs/docs/security/air-gap.md` (2 hits) — replace product name / component.
- `docs/docs/security/audit-log.md` (2 hits) — replace product name / component.
- `docs/docs/security/rbac-hardening.md` (1 hit) — single substitution.
- `docs/docs/runbooks/faq.md` (2 hits) — replace product name / CLI name.
- `docs/docs/runbooks/foundry.md` (1 hit) — single substitution.
- `docs/docs/developer/contributing.md` (4 hits) — technical contributor guide. Replace component names in prose. Preserve code examples verbatim.
- `docs/docs/developer/setup-deployment.md` (3 hits) — deployment guide. Replace component names in prose. Do NOT change service names in compose.server.yaml examples or Docker image names.
- `docs/docs/api-reference/index.md` (1 hit) — single substitution.

After all substitutions, run the zero-hit verification:
```bash
grep -r "mop-push\|Master of Puppets\|\bMoP\b" docs/docs/ --include="*.md"
```
This must return no output.
  </action>
  <verify>
    <automated>! grep -r "mop-push\|Master of Puppets" /home/thomas/Development/master_of_puppets/docs/docs/ --include="*.md" -l 2>/dev/null | grep . && echo "PASS — zero mop-push or Master of Puppets hits in docs/docs/"</automated>
  </verify>
  <done>
    All 16 files updated. Zero occurrences of `mop-push`, `Master of Puppets` remain in docs/docs/. The `Puppeteer` and `Puppet` component terms are replaced with Axiom equivalents in prose (code blocks with internal identifiers left unchanged).
  </done>
</task>

</tasks>

<verification>
Full docs site naming pass verification:

```bash
# Must return zero results
grep -r "mop-push\|Master of Puppets\|\bMoP\b" docs/docs/ --include="*.md"

# mkdocs.yml checks
grep "site_name: Axiom" docs/mkdocs.yml
grep "axiom-push CLI: feature-guides/axiom-push.md" docs/mkdocs.yml

# File rename check
test ! -f docs/docs/feature-guides/mop-push.md && echo "old file gone"
test -f docs/docs/feature-guides/axiom-push.md && echo "new file exists"
```

Docker build (definitive strict mode test — run if docker available):
```bash
docker compose -f puppeteer/compose.server.yaml build docs
```
</verification>

<success_criteria>
- `grep -r "mop-push" docs/docs/` returns zero matches
- `grep -r "Master of Puppets" docs/docs/` returns zero matches
- `docs/docs/feature-guides/mop-push.md` does not exist
- `docs/docs/feature-guides/axiom-push.md` exists
- `docs/mkdocs.yml` has `site_name: Axiom`
- `docs/mkdocs.yml` nav references `axiom-push CLI: feature-guides/axiom-push.md`
- No Python source files modified
- No API route paths modified
</success_criteria>

<output>
After completion, create `.planning/phases/26-axiom-branding-community-foundation/26-03-SUMMARY.md`
</output>
