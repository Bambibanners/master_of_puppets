---
phase: 24-extended-feature-guides-security
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/mkdocs.yml
  - docs/docs/feature-guides/job-scheduling.md
  - docs/docs/feature-guides/rbac.md
  - docs/docs/feature-guides/oauth.md
  - docs/docs/feature-guides/rbac-reference.md
  - docs/docs/security/index.md
  - docs/docs/security/mtls.md
  - docs/docs/security/rbac-hardening.md
  - docs/docs/security/audit-log.md
  - docs/docs/security/air-gap.md
autonomous: true
requirements:
  - FEAT-03
  - FEAT-04
  - FEAT-05
  - SECU-01
  - SECU-02
  - SECU-03
  - SECU-04

must_haves:
  truths:
    - "All 9 new doc files exist on disk with at least a title and one sentence of content"
    - "mkdocs.yml nav includes all 9 new pages under Feature Guides and Security sections"
    - "Local mkdocs build emits no warnings about missing files (non-strict, no openapi.json required)"
    - "Docker build of the docs container passes mkdocs build --strict without errors from the new nav entries"
  artifacts:
    - path: "docs/mkdocs.yml"
      provides: "Updated nav with Feature Guides Platform Config + Reference subsections and full Security section"
      contains: "job-scheduling.md, rbac.md, oauth.md, rbac-reference.md, mtls.md, rbac-hardening.md, audit-log.md, air-gap.md"
    - path: "docs/docs/feature-guides/job-scheduling.md"
      provides: "Stub for FEAT-03 plan 02 to fill"
      min_lines: 3
    - path: "docs/docs/feature-guides/rbac.md"
      provides: "Stub for FEAT-04 plan 03 to fill"
      min_lines: 3
    - path: "docs/docs/feature-guides/oauth.md"
      provides: "Stub for FEAT-05 plan 02 to fill"
      min_lines: 3
    - path: "docs/docs/feature-guides/rbac-reference.md"
      provides: "Stub for FEAT-04 plan 03 to fill"
      min_lines: 3
    - path: "docs/docs/security/index.md"
      provides: "Stub for SECU overview plan 04 to fill (replaces current 'Coming soon' stub)"
      min_lines: 3
    - path: "docs/docs/security/mtls.md"
      provides: "Stub for SECU-01 plan 04 to fill"
      min_lines: 3
    - path: "docs/docs/security/rbac-hardening.md"
      provides: "Stub for SECU-02 plan 05 to fill"
      min_lines: 3
    - path: "docs/docs/security/audit-log.md"
      provides: "Stub for SECU-03 plan 05 to fill"
      min_lines: 3
    - path: "docs/docs/security/air-gap.md"
      provides: "Stub for SECU-04 plan 05 to fill"
      min_lines: 3
  key_links:
    - from: "docs/mkdocs.yml"
      to: "docs/docs/feature-guides/job-scheduling.md"
      via: "nav entry resolves to file"
      pattern: "job-scheduling\\.md"
    - from: "docs/mkdocs.yml"
      to: "docs/docs/security/mtls.md"
      via: "nav entry resolves to file"
      pattern: "mtls\\.md"
---

<objective>
Update mkdocs.yml nav to add all Phase 24 pages and create stub files for each, so the Docker docs build passes mkdocs --strict before any content is written.

Purpose: mkdocs build --strict is enforced in the Dockerfile builder stage. Any nav entry that references a non-existent file fails the build. This plan uses the stub-first pattern established in Phase 23 — create all files before plans 02-05 fill their content.
Output: 9 stub files (8 new + 1 replacement), updated mkdocs.yml nav, clean build.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-extended-feature-guides-security/24-CONTEXT.md
@.planning/phases/24-extended-feature-guides-security/24-RESEARCH.md

<interfaces>
<!-- Current mkdocs.yml nav (abridged) — executor replaces Feature Guides and Security sections. -->

Current Feature Guides nav (to expand):
```yaml
- Feature Guides:
    - Operator Tools:
        - mop-push CLI: feature-guides/mop-push.md
    - Platform Config:
        - Foundry: feature-guides/foundry.md
```

Target Feature Guides nav (exact YAML to use):
```yaml
- Feature Guides:
    - Operator Tools:
        - mop-push CLI: feature-guides/mop-push.md
    - Platform Config:
        - Foundry: feature-guides/foundry.md
        - Job Scheduling: feature-guides/job-scheduling.md
        - RBAC: feature-guides/rbac.md
        - OAuth & Authentication: feature-guides/oauth.md
    - Reference:
        - RBAC Permission Reference: feature-guides/rbac-reference.md
```

Current Security nav (to expand):
```yaml
- Security:
    - Overview: security/index.md
```

Target Security nav (exact YAML to use):
```yaml
- Security:
    - Overview: security/index.md
    - mTLS & Certificates: security/mtls.md
    - RBAC Hardening: security/rbac-hardening.md
    - Audit Log: security/audit-log.md
    - Air-Gap Operation: security/air-gap.md
```

Existing stub content pattern (from security/index.md current):
- Title + one sentence + "Coming soon" — this is sufficient for a stub
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update mkdocs.yml with Phase 24 nav entries</name>
  <files>docs/mkdocs.yml</files>
  <action>
Replace the Feature Guides nav section and the Security nav section in docs/mkdocs.yml with the exact YAML shown in the interfaces block above.

Feature Guides section: Add "Job Scheduling", "RBAC", "OAuth & Authentication" under Platform Config; add a new "Reference" subsection with "RBAC Permission Reference".

Security section: Add "mTLS & Certificates", "RBAC Hardening", "Audit Log", "Air-Gap Operation" entries below the existing "Overview: security/index.md".

All other nav sections (Home, Getting Started, Runbooks, Developer, API Reference) remain unchanged.

Do NOT run mkdocs build yet — the stub files must be created first (Task 2).
  </action>
  <verify>
    <automated>grep -c "job-scheduling\|rbac\|oauth\|mtls\|rbac-hardening\|audit-log\|air-gap" /home/thomas/Development/master_of_puppets/docs/mkdocs.yml</automated>
  </verify>
  <done>docs/mkdocs.yml contains all 8 new nav entries across Feature Guides and Security sections</done>
</task>

<task type="auto">
  <name>Task 2: Create stub files for all 9 Phase 24 pages</name>
  <files>
    docs/docs/feature-guides/job-scheduling.md,
    docs/docs/feature-guides/rbac.md,
    docs/docs/feature-guides/oauth.md,
    docs/docs/feature-guides/rbac-reference.md,
    docs/docs/security/index.md,
    docs/docs/security/mtls.md,
    docs/docs/security/rbac-hardening.md,
    docs/docs/security/audit-log.md,
    docs/docs/security/air-gap.md
  </files>
  <action>
Create the following stub files. Each stub is 3 lines: H1 title, blank line, one-sentence purpose statement. Use the titles as they appear in the nav.

**docs/docs/feature-guides/job-scheduling.md**
```markdown
# Job Scheduling

This guide covers creating scheduled job definitions, configuring cron expressions, targeting nodes by capability, and managing the job lifecycle.
```

**docs/docs/feature-guides/rbac.md**
```markdown
# Role-Based Access Control

This guide covers creating and managing users, assigning roles, configuring service principals, and controlling operator access through the dashboard.
```

**docs/docs/feature-guides/oauth.md**
```markdown
# OAuth & Authentication

This guide covers the device flow authentication process, token lifecycle management, API key usage, and CI/CD integration patterns.
```

**docs/docs/feature-guides/rbac-reference.md**
```markdown
# RBAC Permission Reference

Complete reference table of all permissions available in Master of Puppets and their default assignments across the admin, operator, and viewer roles.
```

**docs/docs/security/index.md** (REPLACE existing stub, do not append):
```markdown
# Security Overview

This section covers the defence-in-depth security model of Master of Puppets, including mTLS certificate management, role-based access control hardening, audit logging, and air-gap operation.
```

**docs/docs/security/mtls.md**
```markdown
# mTLS & Certificates

This guide covers the Root CA setup, JOIN_TOKEN generation, node enrollment, certificate revocation, and the complete cert rotation procedure.
```

**docs/docs/security/rbac-hardening.md**
```markdown
# RBAC Hardening

Prescriptive guidance for configuring least-privilege access, auditing permission assignments, and hardening the access control model for production deployments.
```

**docs/docs/security/audit-log.md**
```markdown
# Audit Log

This guide covers the complete audit event inventory, how to access the audit log, and query patterns for compliance reporting.
```

**docs/docs/security/air-gap.md**
```markdown
# Air-Gap Operation

This guide covers deploying Master of Puppets in a network-isolated environment, including package mirroring setup, offline build validation, and the network isolation checklist.
```

After creating all files, run: `cd /home/thomas/Development/master_of_puppets/docs && mkdocs build 2>&1 | grep -i warning`

Expect zero warnings about missing documentation files. The only expected warning (if any) is the pre-existing openapi.json issue — any "Documentation file" warning is a failure.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/docs && mkdocs build 2>&1 | grep -i "documentation file" | wc -l</automated>
  </verify>
  <done>All 9 stub files exist; mkdocs build emits 0 "Documentation file" warnings; nav entries all resolve</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `ls docs/docs/feature-guides/` shows: foundry.md, job-scheduling.md, mop-push.md, oauth.md, rbac-reference.md, rbac.md
2. `ls docs/docs/security/` shows: air-gap.md, audit-log.md, index.md, mtls.md, rbac-hardening.md
3. `cd docs && mkdocs build 2>&1 | grep -i "documentation file"` returns empty output
4. `grep -c "job-scheduling\|rbac\|oauth\|mtls\|rbac-hardening\|audit-log\|air-gap" docs/mkdocs.yml` returns 8
</verification>

<success_criteria>
- docs/mkdocs.yml updated with all 8 new nav entries
- 8 new stub files created + security/index.md stub replaced
- `mkdocs build` emits no "Documentation file" warnings
- Plans 02-05 can proceed in parallel without nav conflicts
</success_criteria>

<output>
After completion, create `.planning/phases/24-extended-feature-guides-security/24-01-SUMMARY.md`
</output>
