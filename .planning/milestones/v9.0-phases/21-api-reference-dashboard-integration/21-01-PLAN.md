---
phase: 21-api-reference-dashboard-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - puppeteer/agent_service/main.py
  - puppeteer/scripts/export_openapi.py
  - docs/requirements.txt
  - docs/mkdocs.yml
  - docs/Dockerfile
  - docs/docs/api-reference/index.md
  - puppeteer/compose.server.yaml
autonomous: false
requirements:
  - APIREF-01
  - APIREF-02
  - APIREF-03

must_haves:
  truths:
    - "Running `docker compose build docs` succeeds without error and produces an image that contains openapi.json under /usr/share/nginx/html"
    - "Running `export_openapi.py` locally without a server produces a valid openapi.json listing all FastAPI endpoints"
    - "All routes in main.py have a non-default tag — no endpoint falls into the 'default' group in Swagger UI"
    - "The /docs/api-reference/ page embeds Swagger UI with no requests to external CDNs"
  artifacts:
    - path: "puppeteer/scripts/export_openapi.py"
      provides: "Export script — calls app.openapi(), writes JSON, runs without a server"
      exports: ["__main__"]
    - path: "docs/docs/api-reference/index.md"
      provides: "MkDocs page with swagger-ui tag pointing to openapi.json"
      contains: "<swagger-ui src="
    - path: "docs/Dockerfile"
      provides: "Builder stage that installs FastAPI deps, runs export script, then mkdocs build"
      contains: "export_openapi.py"
    - path: "docs/mkdocs.yml"
      provides: "MkDocs config with swagger-ui-tag plugin"
      contains: "swagger-ui-tag"
  key_links:
    - from: "docs/Dockerfile"
      to: "puppeteer/agent_service/main.py"
      via: "COPY puppeteer/agent_service /tmp/agent_service + python /tmp/export_openapi.py"
      pattern: "export_openapi\\.py"
    - from: "docs/docs/api-reference/index.md"
      to: "docs/docs/api-reference/openapi.json"
      via: "swagger-ui src attribute"
      pattern: "src=\"openapi\\.json\""
    - from: "puppeteer/compose.server.yaml"
      to: "docs/Dockerfile"
      via: "build context pointing to repo root"
      pattern: "context: \\.\\."
---

<objective>
Build the OpenAPI export pipeline: tag all FastAPI routes for meaningful grouping, create
an export script that generates openapi.json without a running server, wire it into the
MkDocs Docker build, and configure mkdocs-swagger-ui-tag to render the API reference
page with bundled Swagger UI assets (no CDN).

Purpose: Delivers APIREF-01/02/03 — the /docs/api-reference/ page auto-generates from
the live FastAPI schema at container build time with all endpoints grouped by tag.
Output: export_openapi.py, api-reference/index.md, updated Dockerfile + mkdocs.yml +
compose.server.yaml + requirements.txt, and all routes in main.py tagged.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-api-reference-dashboard-integration/21-RESEARCH.md

# Phase 20 delivered this infrastructure (already live):
# - docs/Dockerfile (python:3.12-slim builder + nginx:alpine serve) — currently: context ../docs
# - docs/mkdocs.yml (Material theme, plugins: search + privacy + offline, --strict build)
# - puppeteer/compose.server.yaml docs service (context: ../docs, dockerfile: Dockerfile)
# - Caddy routes /docs/* to docs nginx container via handle /docs/* (not handle_path)

<interfaces>
<!-- Key import-time behavior in puppeteer/agent_service/ the executor must know. -->

From puppeteer/agent_service/db.py (module-level, runs at import):
  create_async_engine(DATABASE_URL)   # reads DATABASE_URL env var at import time

From puppeteer/agent_service/security.py (module-level, runs at import):
  cipher_suite = Fernet(ENCRYPTION_KEY)  # raises ValueError if ENCRYPTION_KEY is invalid

Dummy env vars required for export script (both in Dockerfile RUN and local test):
  DATABASE_URL=sqlite+aiosqlite:///./dummy.db
  ENCRYPTION_KEY=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
  (that is 43 chars of 'A' + '=' — valid 32-byte url-safe base64 Fernet key)

From docs/Dockerfile (current state):
  FROM python:3.12-slim AS builder
  WORKDIR /docs
  COPY requirements.txt .
  RUN pip install --no-cache-dir -r requirements.txt
  COPY . .
  RUN mkdocs build --strict
  FROM nginx:alpine
  COPY --from=builder /docs/site /usr/share/nginx/html
  COPY nginx.conf /etc/nginx/conf.d/default.conf

From puppeteer/compose.server.yaml docs service (current state):
  docs:
    image: localhost/master-of-puppets-docs:v1
    build:
      context: ../docs        # <-- must change to .. (repo root)
      dockerfile: Dockerfile  # <-- must change to docs/Dockerfile

Currently tagged routes in main.py (22 routes, lines 2032-3060):
  - tags=["Foundry"]        (3 routes: /api/templates/{id}/status, /api/templates/{id}/bom, /api/foundry/search-packages)
  - tags=["Smelter Registry"]  (9 routes)
  - tags=["Headless Automation"]  (10 routes)
  All other ~111 routes have NO tags — fall into "default" group.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tag all untagged routes in main.py and create export_openapi.py</name>
  <files>puppeteer/agent_service/main.py, puppeteer/scripts/export_openapi.py</files>
  <behavior>
    - Test 1: export_openapi.py runs with dummy env vars and writes valid JSON to a file
    - Test 2: The produced JSON contains no path whose only tag is "default"
    - Test 3: The produced JSON contains paths under at least 10 distinct tag groups
    - Test 4: Path count in exported JSON matches total @app. route count in main.py (minus websocket)
  </behavior>
  <action>
Step A — Create puppeteer/scripts/export_openapi.py:

```python
#!/usr/bin/env python3
"""
Export FastAPI OpenAPI schema without running a server.
Run from repo root:
  DATABASE_URL=sqlite+aiosqlite:///./dummy.db \
  ENCRYPTION_KEY=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA= \
  python puppeteer/scripts/export_openapi.py /tmp/out.json
"""
import sys
import json
import os

# Ensure puppeteer/ is on sys.path so agent_service is importable
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))

from agent_service.main import app

schema = app.openapi()

out = sys.argv[1] if len(sys.argv) > 1 else "openapi.json"
os.makedirs(os.path.dirname(os.path.abspath(out)) or ".", exist_ok=True)
with open(out, "w") as f:
    json.dump(schema, f, indent=2)
print(f"openapi.json written: {len(schema['paths'])} paths")
```

Step B — Add tags to ALL untagged routes in main.py. Add `tags=["<group>"]` parameter
to each @app decorator that currently lacks it. Use these tag groups based on route path:

  /api/alerts*, /api/webhooks*                         → tags=["Alerts & Webhooks"]
  /api/executions*                                     → tags=["Execution Records"]
  /api/node/compose, /api/installer/compose            → tags=["System"]
  /verification-key, /config/public-key                → tags=["System"]
  /installer, /installer.sh                            → tags=["System"]
  /system/root-ca*, /system/crl.pem                    → tags=["System"]
  /auth/device*, /auth/login, /auth/register           → tags=["Authentication"]
  /auth/me*, /auth/token                               → tags=["Authentication"]
  /admin/service-principals*                           → tags=["Service Principals"]
  /admin/users*, /admin/roles*                         → tags=["User Management"]
  /admin/audit-log                                     → tags=["Audit Log"]
  /admin/mark-base-updated, /admin/base-image-updated  → tags=["Admin"]
  /admin/generate-token, /admin/upload-key             → tags=["Admin"]
  /jobs (not /jobs/definitions*), /jobs/count          → tags=["Jobs"]
  /api/jobs/stats, /api/jobs/push                      → tags=["Jobs"]
  /jobs/{guid}/*                                       → tags=["Jobs"]
  /work/pull, /work/{guid}/result                      → tags=["Node Agent"]
  /heartbeat                                           → tags=["Node Agent"]
  /nodes*, /api/nodes*                                 → tags=["Nodes"]
  /api/enroll, /api/enrollment-tokens                  → tags=["Node Agent"]
  /api/blueprints*, /foundry/definitions               → tags=["Foundry"]
  /api/templates*, /foundry/build                      → tags=["Foundry"]
  /api/capability-matrix*, /api/approved-os*           → tags=["Foundry"]
  /api/images*                                         → tags=["Foundry"]
  /job-definitions, /jobs/definitions*                 → tags=["Job Definitions"]
  /signatures*                                         → tags=["Signatures"]
  /config/mounts*                                      → tags=["System"]
  /api/artifacts*                                      → tags=["Artifacts"]
  /api/docs, /api/docs/{filename}                      → tags=["System"]
  /                                                    → tags=["System"]
  /ws (websocket)                                      → no tags needed (websocket, exclude)

After adding tags, verify by running the export script locally:
  cd /home/thomas/Development/master_of_puppets
  DATABASE_URL=sqlite+aiosqlite:///./dummy.db \
  ENCRYPTION_KEY=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA= \
  python puppeteer/scripts/export_openapi.py /tmp/openapi_test.json

Confirm: script exits 0, /tmp/openapi_test.json exists, and no path has only ["default"] as tag.
  python -c "
import json
s = json.load(open('/tmp/openapi_test.json'))
defaults = [p for p, methods in s['paths'].items()
            for m, d in methods.items() if m != 'parameters'
            and d.get('tags') == ['default']]
assert not defaults, f'Untagged paths: {defaults}'
print(f'OK: {len(s[\"paths\"])} paths, no default-only tags')
"
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets && DATABASE_URL=sqlite+aiosqlite:///./dummy.db ENCRYPTION_KEY=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA= python puppeteer/scripts/export_openapi.py /tmp/openapi_verify.json && python -c "import json; s=json.load(open('/tmp/openapi_verify.json')); bad=[p for p,ms in s['paths'].items() for m,d in ms.items() if m!='parameters' and d.get('tags')==['default']]; assert not bad, f'Default-only: {bad[:5]}'; print(f'PASS: {len(s[\"paths\"])} paths exported, no default-only tags')"</automated>
  </verify>
  <done>export_openapi.py exists and runs clean. All routes in main.py have a meaningful tag. Exported JSON contains no default-only tagged paths.</done>
</task>

<task type="auto">
  <name>Task 2: Wire export into Dockerfile, add swagger-ui-tag to MkDocs, update compose build context</name>
  <files>docs/Dockerfile, docs/requirements.txt, docs/mkdocs.yml, docs/docs/api-reference/index.md, puppeteer/compose.server.yaml</files>
  <action>
Step A — Update docs/requirements.txt — add mkdocs-swagger-ui-tag:
  mkdocs-material==9.7.5
  mkdocs-swagger-ui-tag==0.8.0

Step B — Update docs/mkdocs.yml — add swagger-ui-tag to plugins list:
  plugins:
    - search
    - privacy
    - offline
    - swagger-ui-tag
  Do NOT add a nav: section — leave it absent so MkDocs auto-discovers pages
  and --strict does not fail on undeclared pages. (Nav is locked in Phase 23.)

Step C — Create docs/docs/api-reference/index.md:
  # API Reference

  Full REST API for Master of Puppets. All endpoints require JWT authentication
  except node-facing endpoints (enroll, work/pull, heartbeat) which use mTLS.

  <swagger-ui src="openapi.json" validatorUrl="none"/>

  Note: validatorUrl="none" is REQUIRED — without it Swagger UI POSTs to
  validator.swagger.io on each page load, breaking the air-gap requirement.

Step D — Update docs/Dockerfile. The build context is changing to repo root, so all
COPY paths must be relative to repo root. Replace the entire Dockerfile:

  FROM python:3.12-slim AS builder
  WORKDIR /docs

  # Install MkDocs dependencies
  COPY docs/requirements.txt requirements.txt
  RUN pip install --no-cache-dir -r requirements.txt

  # Install FastAPI app dependencies (needed for export script to import agent_service)
  COPY puppeteer/requirements.txt /tmp/app-requirements.txt
  RUN pip install --no-cache-dir -r /tmp/app-requirements.txt

  # Copy MkDocs source
  COPY docs/ .

  # Copy export script and agent_service for OpenAPI generation
  COPY puppeteer/scripts/export_openapi.py /tmp/export_openapi.py
  COPY puppeteer/agent_service /tmp/agent_service

  # Generate openapi.json from FastAPI app (no running server required)
  # DATABASE_URL: prevents asyncpg connection-string validation at engine creation
  # ENCRYPTION_KEY: prevents Fernet(key) import-time failure in security.py
  RUN DATABASE_URL=sqlite+aiosqlite:///./dummy.db \
      ENCRYPTION_KEY=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA= \
      PYTHONPATH=/tmp \
      python /tmp/export_openapi.py docs/api-reference/openapi.json

  # Build MkDocs site (--strict: any warning = build failure)
  RUN mkdocs build --strict

  FROM nginx:alpine
  COPY --from=builder /docs/site /usr/share/nginx/html
  COPY docs/nginx.conf /etc/nginx/conf.d/default.conf
  EXPOSE 80
  CMD ["nginx", "-g", "daemon off;"]

Note on PYTHONPATH=/tmp: the export script does sys.path.insert(0, "..") relative to its
location. When run as /tmp/export_openapi.py, ".." is "/". Setting PYTHONPATH=/tmp ensures
/tmp is on the path so `from agent_service.main import app` resolves to /tmp/agent_service.

Step E — Update puppeteer/compose.server.yaml docs service build context:
  docs:
    image: localhost/master-of-puppets-docs:v1
    build:
      context: ..           # repo root (was: ../docs)
      dockerfile: docs/Dockerfile
    restart: always

Step F — Verify the full build works:
  cd /home/thomas/Development/master_of_puppets/puppeteer
  docker compose -f compose.server.yaml build docs

If build fails due to offline plugin attempting downloads, check whether
mkdocs-swagger-ui-tag bundles its assets correctly (it should — all JS/CSS are
included in the package). If the privacy plugin tries to download Swagger UI assets
from the plugin's bundled HTML and fails, add:
  - privacy:
      enabled: !ENV [CI, false]
but only as a fallback — the standard configuration should work.
  </action>
  <verify>
    <automated>cd /home/thomas/Development/master_of_puppets/puppeteer && docker compose -f compose.server.yaml build docs && docker run --rm localhost/master-of-puppets-docs:v1 find /usr/share/nginx/html -name "openapi.json" | grep -q "openapi.json" && echo "PASS: openapi.json embedded in built image"</automated>
  </verify>
  <done>docker compose build docs succeeds. The built image contains /usr/share/nginx/html/api-reference/openapi.json (or similar path). The /docs/api-reference/ page is included in the static site with the swagger-ui tag.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify API reference renders correctly in browser</name>
  <what-built>
    OpenAPI export pipeline: all 133 routes in main.py tagged with meaningful groups,
    export_openapi.py generates openapi.json without a running server, Docker build
    integrates export before mkdocs build, swagger-ui-tag renders the API reference
    page with bundled assets.
  </what-built>
  <how-to-verify>
    1. Start the docs container: cd puppeteer && docker compose -f compose.server.yaml up -d docs
    2. Open https://dev.master-of-puppets.work/docs/api-reference/ in a browser
    3. Verify: Swagger UI renders with endpoint groups visible (Authentication, Jobs, Nodes, etc.)
    4. Verify: Open browser DevTools Network tab — filter by domain — confirm NO requests to
       unpkg.com, jsdelivr.net, cdnjs.cloudflare.com, or validator.swagger.io
    5. Verify: Expand one endpoint (e.g., POST /auth/login) — request/response schema is visible
    6. Verify: No "default" group appears in the tag list
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues</resume-signal>
  <action>Human verification — follow how-to-verify steps above.</action>
  <verify><automated>echo "human-checkpoint"</automated></verify>
  <done>User confirms all how-to-verify steps passed and types approved.</done>
</task>

</tasks>

<verification>
Overall phase checks for Plan 21-01:
- `docker compose -f puppeteer/compose.server.yaml build docs` exits 0
- `docker run --rm localhost/master-of-puppets-docs:v1 find /usr/share/nginx/html -name "openapi.json"` returns a path
- `DATABASE_URL=sqlite+aiosqlite:///./dummy.db ENCRYPTION_KEY=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA= python puppeteer/scripts/export_openapi.py /tmp/v.json && python -c "import json; s=json.load(open('/tmp/v.json')); print(len(s['paths']), 'paths')"` prints a count > 100
- No route in the exported JSON has `tags: ["default"]` as its only tag
</verification>

<success_criteria>
1. export_openapi.py runs with two dummy env vars and produces valid JSON listing all FastAPI paths
2. `docker compose build docs` succeeds end-to-end (pip install + export + mkdocs build --strict)
3. Built image contains openapi.json under the MkDocs-built static site
4. /docs/api-reference/ page in the running container renders Swagger UI with no CDN requests
5. All endpoints are grouped under meaningful tags — no "default" group in the UI
</success_criteria>

<output>
After completion, create `.planning/phases/21-api-reference-dashboard-integration/21-01-SUMMARY.md`
</output>
