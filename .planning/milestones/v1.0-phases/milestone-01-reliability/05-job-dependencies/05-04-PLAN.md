---
phase: 05-job-dependencies
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - puppeteer/dashboard/src/views/Jobs.tsx
autonomous: true
requirements:
  - DEP-04

must_haves:
  truths:
    - "Jobs view displays BLOCKED status badge with custom icon"
    - "JobDetailPanel includes a Dependencies section"
    - "Panel lists upstream job GUIDs and their current status"
  artifacts:
    - path: "puppeteer/dashboard/src/views/Jobs.tsx"
      provides: "Dependency visualization in dashboard"
      contains: "status === 'BLOCKED'"
---

<objective>
Enhance the dashboard to visualize job dependencies and provide visibility into blocked tasks.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/PROJECT.md
@/home/thomas/Development/master_of_puppets/.planning/ROADMAP.md
@/home/thomas/Development/master_of_puppets/.planning/phases/05-job-dependencies/05-CONTEXT.md
@/home/thomas/Development/master_of_puppets/.planning/phases/05-job-dependencies/05-RESEARCH.md

<interfaces>
From puppeteer/dashboard/src/views/Jobs.tsx:
```tsx
const getStatusVariant = (status: string) => {
    switch (status.toLowerCase()) {
        case 'completed': return 'success';
        case 'failed': return 'destructive';
        // ...
    }
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BLOCKED status variant and icon</name>
  <files>puppeteer/dashboard/src/views/Jobs.tsx</files>
  <action>
    Update `getStatusVariant` and `StatusIcon` (if applicable) to handle the `BLOCKED` status. Use a Lock icon and neutral/amber styling.
  </action>
  <verify>
    <automated>grep -q "blocked" puppeteer/dashboard/src/views/Jobs.tsx && echo "UI Logic OK"</automated>
  </verify>
  <done>Blocked status is visually distinct in the dashboard</done>
</task>

<task type="auto">
  <name>Task 2: Implement Dependencies section in JobDetailPanel</name>
  <files>puppeteer/dashboard/src/views/Jobs.tsx</files>
  <behavior>
    - Section title: "Dependencies"
    - If job has `depends_on`:
        - Map through GUIDs.
        - Display each GUID as a clickable link or status badge.
        - (Optional) Fetch status of upstreams to show "Waiting for [GUID] (FAILED)".
  </behavior>
  <action>
    Modify `JobDetailPanel` in `puppeteer/dashboard/src/views/Jobs.tsx` to display the `depends_on` list.

    ```tsx
    {job.depends_on && job.depends_on.length > 0 && (
        <div className="space-y-2 mt-4">
            <h4 className="text-xs font-bold text-zinc-500 uppercase tracking-widest px-1">Depends On</h4>
            <div className="space-y-1.5">
                {job.depends_on.map(guid => (
                    <div key={guid} className="flex items-center justify-between bg-zinc-900/50 p-2 rounded border border-zinc-800">
                        <span className="font-mono text-xs text-zinc-400">{guid.slice(0, 8)}...</span>
                        {/* Status fetching could be added here or in a separate component */}
                    </div>
                ))}
            </div>
        </div>
    )}
    ```
  </action>
  <verify>
    <automated>grep -q "Depends On" puppeteer/dashboard/src/views/Jobs.tsx && echo "UI Section OK"</automated>
  </verify>
  <done>Dependency list is visible in job details</done>
</task>

</tasks>

<verification>
1. Open dashboard, verify `BLOCKED` jobs show the correct badge.
2. Select a blocked job, verify the "Depends On" section correctly lists the upstream GUIDs.
</verification>

<success_criteria>
- Operators can easily identify why a job is blocked.
- Dependency relationships are visible in the UI.
</success_criteria>

<output>
After completion, create `.planning/phases/05-job-dependencies/05-04-SUMMARY.md`
</output>
