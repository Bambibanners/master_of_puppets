---
phase: 04-environment-tags
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - puppeteer/agent_service/main.py
  - puppeteer/dashboard/src/components/AddNodeModal.tsx
  - puppeteer/installer/install_universal.ps1
gap_closure: true
autonomous: true
requirements:
  - TAG-01 (expansion)

must_haves:
  truths:
    - "AddNodeModal includes a tags input field"
    - "One-liner command includes -Tags parameter"
    - "Installer script passes Tags to the compose generator"
    - "Server injects custom tags into the generated node-compose.yaml"
  artifacts:
    - path: "puppeteer/dashboard/src/components/AddNodeModal.tsx"
      provides: "Tags input in provisioning UI"
---

<objective>
Allow operators to specify initial tags when provisioning new nodes, ensuring they enter the mesh with the correct environment labeling.
</objective>

<execution_context>
@/home/thomas/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/thomas/Development/master_of_puppets/.planning/PROJECT.md
@/home/thomas/Development/master_of_puppets/.planning/phases/04-environment-tags/04-CONTEXT.md
@/home/thomas/Development/master_of_puppets/.planning/phases/04-environment-tags/04-UAT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update node compose generator in main.py</name>
  <files>puppeteer/agent_service/main.py</files>
  <action>
    Modify `get_node_compose` to:
    1. Accept an optional `tags` query parameter.
    2. Inject it into the `NODE_TAGS` env var in the YAML template.
    3. Alias the route to also support `/api/node/compose` (fixing the script mismatch).

    ```python
    @app.get("/api/node/compose")
    @app.get("/api/installer/compose")
    async def get_node_compose(token: str, mounts: Optional[str] = None, tags: Optional[str] = None):
        effective_tags = tags if tags else "general,linux,arm64"
        # ... template uses {effective_tags} for NODE_TAGS ...
    ```
  </action>
  <verify>
    <automated>grep -q "effective_tags = tags" puppeteer/agent_service/main.py && echo "API Update OK"</automated>
  </verify>
  <done>Compose generator supports dynamic tags</done>
</task>

<task type="auto">
  <name>Task 2: Update AddNodeModal.tsx</name>
  <files>puppeteer/dashboard/src/components/AddNodeModal.tsx</files>
  <action>
    Add a `tags` state and input field. Update `handleCopy` to include the `-Tags` argument in the generated PowerShell command.
  </action>
  <verify>
    <automated>grep -q "Tags" puppeteer/dashboard/src/components/AddNodeModal.tsx && echo "UI Update OK"</automated>
  </verify>
  <done>Provisioning UI allows setting initial tags</done>
</task>

<task type="auto">
  <name>Task 3: Update install_universal.ps1</name>
  <files>puppeteer/installer/install_universal.ps1</files>
  <action>
    Add `[string]$Tags` to the `param()` block.
    Append `&tags=$Tags` to the `$ComposeUrl` if tags are provided.
  </action>
  <verify>
    <automated>grep -q "string.Tags" puppeteer/installer/install_universal.ps1 && echo "Installer Update OK"</automated>
  </verify>
  <done>Universal installer supports -Tags parameter</done>
</task>

</tasks>

<verification>
1. Open Provision Node modal. Enter `env:prod` in tags.
2. Verify the one-liner ends with `-Tags "env:prod"`.
3. Verify the server generates a YAML with `NODE_TAGS=env:prod` when called with `?tags=env:prod`.
</verification>

<success_criteria>
- Tags specified at provisioning time are correctly propagated to the node's environment.
- The server/script URL mismatch is resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/04-environment-tags/04-05-SUMMARY.md`
</output>
