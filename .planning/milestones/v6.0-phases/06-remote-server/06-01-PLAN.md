# Plan 06-01: Remote Infrastructure Hardening

## Objective
Enable seamless deployment of the Master of Puppets stack on remote Linux environments and ensure robust node enrollment via the universal installer.

## Strategy
1.  **CA Unification**: Modify `PKIService` to prioritize the global cert-manager CA. (DONE)
2.  **Server Provisioning**: Provide `deploy_server.sh` for automated remote setup. (DONE)
3.  **Installer Hardening**: Update `install_universal.sh` to handle remote hostnames and improve bootstrap reliability.
4.  **Documentation**: Create a `REMOTE_DEPLOYMENT.md` guide.

## Requirements Covered
- **REM-01**: Single-script server deployment.
- **REM-02**: Correct `SERVER_HOSTNAME` propagation.
- **REM-03**: Portable Linux installer.

## Tasks
- [x] Unify `agent_service` and `cert-manager` CAs.
- [x] Create `puppeteer/installer/deploy_server.sh`.
- [ ] Refine `puppeteer/installer/install_universal.sh`.
- [ ] Create `docs/REMOTE_DEPLOYMENT.md`.
